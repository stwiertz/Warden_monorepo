# generated: 2026-05-09
# last_updated: 2026-05-17 (Story 9.14 `9-14-roi-detection-tester-refit-for-unified-schema` /bmad-dev-story (Stephane, Amelia): flipped `ready-for-dev → in-progress → review`. Tasks 1–6 + AC0–AC11 `[x]`; full suite **194 passed** (uv + pnpm parity; 132 9.12-baseline + 43 9.13-uncommitted + 19 this story; 0 regressions; 9.13 contract-pins green via the `name` alias). AC12/13/14 + Task 7 `[HELD]` → `story-9-14-postmerge` Two-PR follow-up. Branch `story-9-14-roi-detection-tester-refit` off `main` HEAD `546e467`. Pre-flight gate green (9.9c `9b9d4af` + 9.11 `aca0906` + 9.12 `546e467` on `main`; `tools/common/` + schema + emitter present). BASELINE NOTE: `uv run pytest -q` = **175** not the story-assumed clean 132 — Story 9.13's delivered work is UNCOMMITTED on `main` (`tools/video_test.py` + `tests/test_video_detection_tester.py` [43 tests] + modified `wardentooling.py`/`common/zones.py`; 9.13 status `review`, unmerged, no branch). KICKOFF DECISIONS (AC0): (0a) per-map scoring = **weighted aggregate** (Σ effective_weight×fired; effective_weight = weight_override if not None else weight; argmax ≥ identification_threshold; HUD+in_match stay unweighted fires/n); (0b) `_normalize_hud(s)` = lowercase + strip single trailing `.0` (`v2.0`↔`v2`, idempotent). SCOPE NOTE (9.13 collision): 9.13's uncommitted `video_test.py` imports `ZoneSpec`/`zone_fires_on_frame`/`_argmax_with_threshold`/`_resize_to_ref` from `tools.roi_detection_tester`, constructs `ZoneSpec(name=…, kind="map")`, and contract-pins `.name` + the 3 fns — AC5's literal `name`→`id` rename would break 9.13's 43 uncommitted tests. RESOLUTION (Stephane): soften AC5 to **add `id` + `weight`/`weight_override`, retain `name` read-alias + `name=` kwarg-alias**; develop on the current tree (9.13 contract-net stays live & green); no scope-creep into 9.13; branch/commit boundaries handled at Task 7 [HELD]. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev`.)
# last_updated: 2026-05-17 (Story 9.14 `9-14-roi-detection-tester-refit-for-unified-schema` created via /bmad-create-story (Stephane); flipped `backlog → ready-for-dev`. Story file finalized: 15 ACs (AC0 input-contract decision + new HUD-version classifier AC2 + binary-in_match-replaces-4-way AC3 + per-map AC4 + loader-rewrite-to-unified-Zone AC5 + per-map-scoring decision AC6 + 3-section report.json AC7 + summary.md+empty-zones AC8 + frame_predictions.csv columns AC9 + pytest refit AC10 + wardentooling flow_tool9 AC11 + AC12/13/14 [HELD]), 7 Tasks, exhaustive Dev Notes. CENTRAL DESIGN CALL (AC0): Tool 9 input rebased from the retired Tool-8 `discovered_zones.yaml` (gone post-9.11) to the emitted unified `map_config.<hud_version>.json` (9.9c artifact; `--config` replaces `--zones`) — evidenced by the 9.9b loop (`zone_picker → map_config_emitter → video_test and/or roi_detection_tester`) and 9.13 consuming the same artifact. Two kickoff decisions for Stephane: (0a) per-map scoring weighted-aggregate (effective_weight=weight_override?:weight vs `identification_threshold`; recommended/faithful) vs literal-`fires/n` unchanged; (0b) `v2.0`(disk)↔`v2`(schema enum) `_normalize_hud` rule. KEY FINDINGS: 4-way `{lobby,in_match,score,transition}`+`TARGET_CLASSES` DELETED not extended (Tool 6 labeler + 4-folder layout untouched; binary collapse only in `_folder_to_in_match`); unified `Zone` has `id` not `name`, nested `hsv`, required `min_ratio`/`weight`/`weight_override` (no `.get(...,0.3)` default — missing key = broken-config error the 9.9b loop must surface); HUD-version classifier = per-HUD binary detector vs multi-HUD GT; empty-zones short-circuit (`accuracy 0.000 — zones unpopulated`, exit 0) is the NORMAL early-9.9b state; reuse `tools.common.zones.band_inrange_ratio` verbatim; remove `import yaml`, no jsonschema gate in Tool 9; REL-005 byte-identical output preserved. CROSS-STORY CONSTRAINT: Story 9.13 (`video_test`, also flipped `ready-for-dev` today) imports `zone_fires_on_frame`/`_argmax_with_threshold`/`_resize_to_ref` from `tools.roi_detection_tester` and pins them with a contract test — the refit MUST keep those three symbols' names+signatures frozen; the two stories are parallel-safe in either order. Dependencies SATISFIED — 9.9c `done` (`9b9d4af`) + 9.11 `done` (`aca0906`) + 9.12 `done` (`546e467`, pytest baseline 132) on `main`; 9.14 fully unblocked. Sprint fit: fits-in-one-sprint (~1-2 days; single-file refit, structure preserved, all math reused). All 15 ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; AC12/13/14 `[HELD]`. Two-PR + local `--no-ff` (`story-9-14-roi-detection-tester-refit` → main; `story-9-14-postmerge`) per [[feedback_two_pr_docs_execution]] + 9.9c/9.11/9.12 precedent. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (after 9.14 lands, blocked only on 9.13). No code touched in this skill run.)
# last_updated: 2026-05-17 (Story 9.13 `9-13-video-detection-tester` CREATE-STORY (Stephane, /bmad-create-story on 9.13, Amelia). Flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-13-video-detection-tester.md` finalized: 15 ACs, 9 Tasks, exhaustive Dev Notes. This is the LAST new tool in the Epic 9 consolidation — Tool 11 `video_test.py`, the end-to-end video pipeline (workflow step #6) and 9.9b's real-video acceptance gate. Disaster-prevention findings recorded: (1) `roi_detection_tester.load_zones_fragment` parses a Tool-8 `discovered_zones.yaml` shape, NOT the emitted unified `map_config.<hud>.json` — a dedicated `_load_config` + unified-`Zone`→`ZoneSpec` mapper is mandatory; (2) `HsvBand.min_ratio` 0.3 class default trap — must set `min_ratio=zone["min_ratio"]` per zone; (3) "i-frame extraction" = stride-sampled frames (≈1/sec), NOT codec keyframes (true keyframes need PyAV/ffmpeg = forbidden new dep) — accepted reading recorded as a decision; (4) frames must `_resize_to_ref` to `reference_resolution` height before zone-fire (Tool 9 parity); (5) single `--config` only — multi-config runtime selection is Story 1.13's scope. Hard reuse: `zone_fires_on_frame`/`_argmax_with_threshold`/`_resize_to_ref` from `tools.roi_detection_tester` + `band_inrange_ratio`/`Rect`/`HsvBand` from `tools.common.zones` — zero hue-wrap reinvention, pinned by a contract test against a future 9.14 Tool-9 refit. Dependencies: hard 9.9c `done` (`9b9d4af`) + ideal 9.12 `done` (`54724ed`) both satisfied; 9.14 `backlog` NOT a blocker. Downstream 9.9b `ready-for-dev` consumes video_test as its real-video gate (exact CLI shape preserved). Sprint fit: fits-in-one-sprint (~2-3 days). Track C. All 15 ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; AC12–AC15 `[HELD]`. Two-PR + local `--no-ff` per [[feedback_two_pr_docs_execution]] + 9.9c/9.11/9.12 precedent. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (9.13 now ready-for-dev; still also gated on 9.14 `backlog` — 9.14 is the remaining Epic 9 tooling story before 9.9b can run). No code touched in this skill run.)
# last_updated: 2026-05-17 (Story 9.12 `9-12-unified-zone-picker` MERGED and flipped `review → done` via the /bmad-dev-story post-merge Two-PR follow-up (Stephane, executed by Amelia). AC13 post-merge gate confirmed on `main` HEAD (merge commit `54724ed`): apps/tooling pytest **132 passed**, 0 regressions to the Story-9.11 108 baseline. Story AC13/AC14/AC15 + Task 8 flipped `[x]` (all 15 ACs + 8 Tasks now `[x]`); story Status `done`. Follow-up delivered as a second local `git merge --no-ff story-9-12-postmerge → main`. The Epic 9 dependency chain 9.9c → 9.11 → 9.12 is now satisfied — the Unified Zone Picker (Tool 10) is on `main` and the 9.9b iterative-zone-population loop has its picker. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (still blocked on 9.13 + 9.14, both `backlog`). 9.13/9.14 are the remaining Epic 9 tooling stories before 9.9b can run.)
# last_updated: 2026-05-17 (Story 9.12 `9-12-unified-zone-picker` flipped `in-progress → review` via /bmad-dev-story (Stephane, executed by Amelia). Delivered the Unified Zone Picker package `apps/tooling/tools/zone_picker/` (AC1 split: __init__/__main__ thin CLI/fragments[Tk-free]/variance[Tk-free]/modes/app[Tk]) + `tests/test_zone_picker.py` (24 pure-logic cases, no Tk) + `wardentooling.py` Tool 10 registration (flow_zone_picker + _TOOL_MAP/choices_main/menu_main/_reprompt_source; run_tool needed NO change — `-m` arg-list works via existing `[sys.executable]+args`). AC5/AC6 ported byte-for-byte from recovered git (`overlay_stack_analyzer.py@ba6b326` Welford+circular-Hue; `auto_roi_discoverer/discoverer.py@0c1c656^` derive_band_for_rect/circular_*_cv). AC2 reuse: ImageCanvas verbatim; ROIMode/HSVFilterMode adapter-shimmed (CapturingROIMode subclasses _add_roi→super+capture, reuses _to_ref; SeedableHSVFilterMode seed/read around existing StringVars) — zero coordinate/mask-math fork; HSVEditor confirmed nonexistent. AC3 VideoPlayer verified-negative (PNG-seq input); enumeration mirrors iter_labeled_frames order without importing Tool 9. AC9 anti-clobber load-all/write-all-4 + MAP_LABELS ordering + stable ids; serialize_zone schema-clamps (h_tol→[0,180], bool weight_override→null) since emitter does no coercion. AC10 round-trip green through the unchanged emitter. apps/tooling pytest 108 → **132** (+24 zone_picker, 0 regressions to the 9.11 baseline); `pnpm --filter tooling test` 132 parity; `python -m tools.zone_picker --help` rejects bad args before any tkinter import; `import wardentooling` clean + Tool 10 registered. No new dependencies; no code outside apps/tooling/ touched. `gh` unauthenticatable non-interactively → delivered as local `git merge --no-ff story-9-12-unified-zone-picker → main`; single-delivery + Two-PR follow-up (`story-9-12-postmerge`) for `review → done` + AC13/14/15 + Task 8 post-merge box-flips per [[feedback_two_pr_docs_execution]] + 9.9c/9.11 precedent. All 15 ACs were `[ ]` per [[feedback_ac_checkbox_tighten]]; AC1–AC12 now `[x]`, AC13/14/15 stay `[ ] [HELD]`. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (still blocked on 9.13/9.14).)
# last_updated: 2026-05-16 (Story 9.12 `9-12-unified-zone-picker` flipped `ready-for-dev → in-progress` via /bmad-dev-story (Stephane, executed by Amelia). Pre-flight (Task 1) green: deps verified on `main` (9.9c `9b9d4af` + 9.11 `aca0906` via `git cat-file`); apps/tooling pytest baseline pinned **108 passed**; live contract surface read (schema/emitter/test_map_config_emitter/common.zones/common.labels/image_inspector.{canvas,modes}/roi_detection_tester.iter_labeled_frames/wardentooling) — story quotes confirmed accurate, key facts: Zone HSV centers/tols are schema integers (HsvBand already int-coerces + emits the 6-key hsv_dict), emitter reads utf-8-sig and emit() sys.exit's on validation fail (round-trip via _load_fragments/_assemble_output/_validate_against_schema), common.zones.Rect has no `name` (minimap roi serialized separately), PNG order = sorted(listdir)×sorted(glob *.png), ROIMode/HSVFilterMode adapter-shimmed not forked. Implementation in flight. epic-9 stays `in-progress`.)
# last_updated: 2026-05-16 (Story 9.12 `9-12-unified-zone-picker` CREATE-STORY (Stephane, /bmad-create-story on 9.12, Amelia). Flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-12-unified-zone-picker.md` finalized with full developer context: 15 ACs, 8 Tasks, exhaustive Dev Notes. Decision recorded: tool is a **package** `apps/tooling/tools/zone_picker/` (not single-file) — pure-logic `variance.py`+`fragments.py` isolated from Tk `app.py`+`modes.py` so AC11's no-Tk-in-tests holds (`image_inspector/` precedent). KEY FINDINGS / disaster-prevention: (1) spec AC2's `HSVEditor` is STALE — that class died with the retired `minimap_zone_selector` (9.11, `0c1c656`); real reuse = `ImageCanvas` + `image_inspector.modes.{HSVFilterMode,ROIMode}`; (2) spec AC3's `VideoPlayer` reuse is a mis-fit — it's a video-file widget, input here is PNG sequences → verified-negative, PNG stepping is in-tool; (3) fragment-clobber hazard — each mode writes only 1 of the 4 emitter-required fragments → AC9 mandates merge-safe load-all/write-all; (4) `config.yaml` zone source is GONE (9.11 `c232924`) — picker reads `output/labeled/`, writes `output/zones/`, never touches config.yaml; (5) `overlay_stack_analyzer` Welford/circular-Hue variance + `auto_roi_discoverer.derive_band_for_rect` auto-seed math RECOVERED from git (`ba6b326`, `0c1c656^`) and quoted verbatim in Dev Notes for PORT-not-invent; (6) contract locked from live `9b9d4af`/`aca0906` `main`: `hud_version` enum `["v1","v2"]` (not "v2.0"), user-space HSV, `weight_override: number|null` (never bool), 4 fragment files `manifest/hud_version_detection/in_match_detection/minimap_identification.json`, emitter does NO coercion (jsonschema gate is the only enforcement). Dependencies SATISFIED — 9.9c `done` (`9b9d4af`) + 9.11 `done` (`aca0906`) on `main`; 9.12 fully unblocked. Sprint fit: fits-in-one-sprint high end (~4–5 focused days); two-sprint risk fenced to AC7's in-tool feedback (capped at fire-ratio/aggregate-score readout — Tool 9/13/14 own authoritative accuracy). All 15 ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; post-merge AC13/14/15 + Task 8 tagged `[HELD]`. Two-PR + local `--no-ff` delivery per [[feedback_two_pr_docs_execution]] + 9.9c/9.11 precedent. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (still blocked on 9.13/9.14). No code touched in this skill run.)
# last_updated: 2026-05-16 (Story 9.11 `9-11-retire-legacy-tooling` MERGED to `main` and flipped `review → done` via the /bmad-dev-story post-merge Two-PR follow-up (Stephane, executed by Amelia). Main delivery: local `git merge --no-ff story-9-11-retire-legacy-tooling → main` → merge commit `aca0906` (41 files, +555/−9720; 7 chunked commits `19b3ec1`→`f6dbbcb`). Post-merge regression on `main` green: apps/tooling pytest 108 passed; `pnpm --filter tooling test` 108 passed. Story AC1–AC9 + Tasks 1–9 `[x]`; Status `done`. Follow-up delivered as a second local `--no-ff` merge `chore: Story 9.11 post-merge follow-up` (branch `story-9-11-postmerge`). The Epic 9 dep chain step 9.9c → 9.11 is now satisfied — the legacy/auto-suggest tooling is retired, shared primitives live in `apps/tooling/tools/common/{labels,zones,labeled_dataset}.py`, and 9.12/9.13/9.14 can now be written against a clean tree. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (still blocked on 9.12/9.13/9.14). AC7 orphan report surfaced `apps/tooling/utils/config.py`+`config.yaml` as a candidate follow-up retirement — NOT acted on (out of 9.11 scope).)
# last_updated: 2026-05-16 (Story 9.11 `9-11-retire-legacy-tooling` flipped `ready-for-dev → in-progress → review` via /bmad-dev-story (Stephane, executed by Amelia). Tasks 1–8 done; AC1–AC7 `[x]`. Relocated shared primitives → `apps/tooling/tools/common/{labels,zones,labeled_dataset}.py` (verbatim + the one sanctioned `_default_input_dir` depth-fix, guarded by new `test_common_labeled_dataset.py`); repointed Tool 6 + Tool 9 + Tool 9's test; scrubbed all stale retired-module prose (tree-wide AC3 grep = 0 in surviving `.py`); `git rm` 9 retire-list entries + 2 `.md` + 2 retired test files + orphaned `astera_expected.json` BSD fixture; fully de-registered Tools 1/2/5/7/8 + 2 Dev flows from `wardentooling.py` (+ removed same-file orphans `import re`/`_EXCLUDED_DIRS`/`browse_directory`); deleted `config.yaml` `minimap_identification` block (lines 87–912; `yaml.safe_load` still OK). apps/tooling pytest 168 → 108 (−63 deleted [30 `test_auto_roi_discoverer` + 33 `test_overlay_stack_analyzer`] +3 mandated depth-fix guard); `pnpm --filter tooling test` 108 green; entrypoint/menu smoke OK (0 retired tokens in `wardentooling.py`). **AC7 orphan report:** `apps/tooling/utils/config.py` (+`load_config`) and `apps/tooling/config/config.yaml` are now FULLY ORPHANED post-retirement → flagged as candidate follow-up retirement, NOT deleted in 9.11 (don't-widen-scope anti-pattern). Delivered as a local `git merge --no-ff story-9-11-retire-legacy-tooling → main` (gh unauthenticatable non-interactively — 9.9c Scope-Adjustment-#5 precedent). AC8/AC9 + Task 9 held `[ ]`/`[HELD]` — sprint-status `review → done` + this story's post-merge box-flips land via the `story-9-11-postmerge` Two-PR follow-up per [[feedback_two_pr_docs_execution]]. epic-9 stays `in-progress`. 9.12/9.13/9.14/9.9b unchanged — still blocked on their own deps; 9.11 landing only removes the "don't import about-to-die modules" hazard.)
# last_updated: 2026-05-16 (Story 9.10 `9-10-prd-architecture-editorial-pass-roi-hsv-pivot` created via /bmad-create-story (Stephane); flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-10-prd-architecture-editorial-pass-roi-hsv-pivot.md` finalized with full developer context: 11 ACs (AC0 rollout decision + AC1 scope-boundary attestation gate + AC2/AC3/AC5 Cohort-A prose scrub + AC4 PRD-V1-treatment decision + AC6 Cohort-B accuracy citations gated on 9.9b + AC7 no-code invariant + AC8 cross-ref integrity + AC9/AC10 sprint-status lifecycle & Two-PR follow-up), 8 Tasks, Dev Notes (schema-of-record framing per 9.9c `done`; THE central anti-pattern = destroying historical-traceability records while scrubbing; Editorial Target Manifest with FROZEN/IN-SCOPE classification across prd.md/architecture.md/epics-and-stories.md). KEY FINDINGS: (1) two-cohort split — Cohort A (prose scrub) is UNBLOCKED by 9.9c `done` (`9b9d4af`); Cohort B (accuracy-floor citations) is GATED on Story 9.9b reporting measured floors; recommended default (AC0 Option A) holds the whole story until 9.9b `done` to avoid double-editing the same paragraphs. (2) architecture.md contains ZERO `oneOf`/v1-v2 prose — the scope-expansion's architecture target is a verify-and-record-negative, not an edit. (3) ~60 stale-prose hits exist but the MAJORITY sit inside deliberately-FROZEN blocks (cancelled stories 9.1/9.2/9.3/9.4, the SUPERSEDED 9.9a block, the preserved original-9.9-stub content per `sprint-status.yaml:183`, and architecture.md's Brownfield-Item-7-as-V1-shipped narrative) — editing them destroys the pivot audit trail; AC1/Task-2 attestation makes the FROZEN boundary non-negotiable. (4) PRD V1 tooling-HASH/LABEL/VALIDATE requirements describe V1-as-shipped pHash reality (V1 genuinely ships pHash) → AC4 default = annotate-with-post-V1-pointer, not destructive reframe. Dev-cycle kickoff held until 9.9b reports per AC0 default. All 11 ACs `[ ]` per [[feedback_ac_checkbox_tighten]]. Two-PR + local `--no-ff` delivery per 9.9c precedent. epic-9 stays `in-progress`. No code/docs touched in this skill run beyond the story file.)
# last_updated: 2026-05-16 (Story 9.11 `9-11-retire-legacy-tooling` created via /bmad-create-story (Stephane); flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-11-retire-legacy-tooling.md` finalized with full developer context. KEY FINDING: the stub's "pure mechanical ~1-day deletion / no cross-package imports" framing under-scoped the work — an import-graph audit showed surviving Tool 6 (`video_timeline_labeler.py`) + Tool 9 (`roi_detection_tester.py`) + Tool 9's test import retire-list modules (`frame_labeler.MAP_LABELS/LABEL_DISPLAY`, `auto_roi_discoverer.model.Rect/HsvBand/TARGET_CLASSES`, `auto_roi_discoverer.validator.band_inrange_ratio`, `overlay_stack_analyzer._default_input_dir`). A naive 9-item delete reds the suite; Tool 6 has no refit story and Tool 9's refit (9.14) lands AFTER 9.11 — so 9.11 must relocate those primitives to `apps/tooling/tools/common/` + repoint survivors BEFORE deleting (AC2/AC3, Task 2/3). 9 ACs, 9 Tasks; re-estimated ~1.5-2 days (still fits-in-one-sprint). Upstream gate 9.9c confirmed `done` (`9b9d4af`) — emitter already decoupled from config.yaml/retired helpers, so AC6 is verify-only. Two create-time decisions: D1 relocation target = `tools/common/` (baked default), D2 config.yaml `minimap_identification` block delete-vs-keep (held `[ ]` for Stephane). All ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; post-merge/decision-pending ACs tagged `[HELD]`. Two-PR + local `--no-ff` delivery per 9.9c precedent. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (still blocked on 9.11/9.12/9.13/9.14). No code touched in this skill run.)
# last_updated: 2026-05-16 (Story 9.9c MERGED to `main` and flipped `review → done`. Scope Adjustment #5 reverses #4's "consolidated Epic 9 PR" deferral per Stephane's explicit 2026-05-16 decision — 9.9c merged independently to unblock the Epic 9 dependency chain (9.9c → 9.11 → 9.12/9.14 → 9.13 → 9.9b), which surfaced when `/bmad-dev-story 9.9b` HALTed on AC1's unsatisfiable hard-dependency gate (0/4 deps `done`; `zone_picker.py`/`video_test.py` nonexistent; legacy tools un-retired). Pre-merge gates green: apps/tooling pytest 168 passed; `pnpm --filter tooling test` 168 passed. `gh` unauthenticatable non-interactively → delivered as a local `git merge --no-ff tooling-refactor → main` (merge commit `9b9d4af`, pushed `890fa01..9b9d4af`); single-delivery shape + Two-PR follow-up (branch `story-9-9c-postmerge`) preserved. 9.9c AC11/AC12/Task 7 flipped `[x]`, Status `done`. epic-9 stays `in-progress`. 9.9b remains `ready-for-dev` — still blocked on 9.11/9.12/9.13/9.14 which do not yet exist.)
# last_updated: 2026-05-15 (Story 9.9c implementation chunked-committed to `tooling-refactor` (commits 4c4ed7f / fe144ca / 91047c1 / f3fc49d for chunks a/b/c/d; chunk e + 9.9b create-story artifact + this header bump follow). PR opening DEFERRED per Stephane's directive to bundle all of Epic 9 (9-11/9-12/9-13/9-14/9-9b/9-10) on the same `tooling-refactor` branch before opening one consolidated PR to `main`. AC11 + AC12 + Task 7 sub-boxes stay `[ ]` for 9.9c — both push and PR are deferred until the Epic 9 single-PR ships. Two-PR follow-up pattern preserved (the `review → done` flip ships post-merge of the consolidated PR). Apps/tooling pytest suite 168 green pre-commit; no new dependencies; no code regressions.)
# last_updated: 2026-05-09 (Story 1.1 AR-SPIKE flipped in-progress → review on branch ar-spike-perf-floor; binding-only cut: AC1/AC2/AC3/AC11/AC12/AC15/AC16-partial closed; AC4/AC5/AC6/AC7/AC8/AC9/AC10-final/AC13 deferred to a new follow-up Story 1.1.1 — AR-SPIKE measurement that runs after Stories 1-13/1-14/1-15/1-16 + new-HUD work + pHash/ahash method reconciliation land. Provisional rung verdict: rung-0 plausible — binding viable; final TBD post-Story-1.1.1; cloud-fallback FORBIDDEN per AC11. Six substrate gaps audited in `_bmad-output/architecture-spike-perf-floor.md`. Reference device re-anchored Poco X5 → Poco X5 Pro 5G (SM7325/A14); cross-cutting cascade through architecture/prd/epics/ux is AC13-extended post-merge admin. Story 1.1.1 entry to be added to sprint-status post-merge. Story 0.2 docs branch in parallel review will merge first per G0; this branch will rebase against main after that.)
# project: Warden_monorepo
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#   - cancelled: Story explicitly de-scoped (decision recorded in last_updated comment)
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-09
last_updated: 2026-05-17  # DEV-STORY 2026-05-17 (Stephane, /bmad-dev-story on 9.14, Amelia): Story 9.14 `9-14-roi-detection-tester-refit-for-unified-schema` delivered via local `git merge --no-ff` to `main`; status `review` (`review -> done` flip lands via the `story-9-14-postmerge` Two-PR follow-up per [[feedback_two_pr_docs_execution]]). Tasks 1-6 + AC0-AC11 `[x]`; full suite 196 passed (uv + pnpm parity, 0 regressions; 9.13 contract-pins green via the `name` alias). Commit-boundary isolated from Story 9.13 (its `video_test.py`/test/`wardentooling` Tool-11/`common/zones.py` deltas remain uncommitted on `main` for its own separate delivery). epic-9 stays `in-progress`. 9.9b stays `ready-for-dev`.
project: Warden_monorepo
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ───────────────────────────────────────────────────────────────────
  # EPIC 0 — Sprint 2.5 Closure & Conflict Audit (G0 GATE for Sprint 3 merges)
  # ───────────────────────────────────────────────────────────────────
  epic-0: in-progress
  0-1-conduct-per-story-conflict-audit-for-sprint-2-5-in-flight-mobile-work: done
  0-2-execute-sprint-2-5-per-story-dispositions: done
  epic-0-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 1 — Foundations: V1-Blocking Spike, Brownfield, Cross-Language Contracts
  # ───────────────────────────────────────────────────────────────────
  epic-1: in-progress
  1-1-pre-prd-performance-spike-ar-spike: done
  1-1-1-ar-spike-measurement: cancelled  # CANCELLED 2026-05-09 (Stephane). Ship-and-observe replaces formal measurement. AC4/AC5/AC6/AC7/AC8/AC9/AC10-final/AC13 dropped as measurement obligations; numeric PERF-002/003/004/005 budgets become soft UX targets verified by manual testing during the build of Stories 5.4/5.5/6.6 + auto-slice on first real run. Rationale: don't optimize what may not need it; build first, observe in V1, optimize only on real complaints.
  1-2-foreground-service-android-config-plugin: ready-for-dev
  1-3-stripe-api-pin-bump: backlog
  1-4-firebase-v12-rn-auth-migration-add-deps-prebuild: backlog
  1-5-firebase-v12-rn-auth-migration-migrate-firebase-config: backlog
  1-6-firebase-v12-rn-auth-migration-migrate-auth-service: backlog
  1-7-firebase-v12-rn-auth-migration-migrate-subscription-service: backlog
  1-8-firebase-v12-rn-auth-migration-migrate-detection-config-service: backlog
  1-9-firebase-v12-rn-auth-migration-end-to-end-manual-test: backlog
  1-10-user-doc-schema-tighten-reconcile: backlog
  1-11-wire-apps-web-to-contracts-user-doc: backlog
  1-12-customer-subscription-updated-defensive-handler: backlog
  1-13-hybrid-map-config-delivery-schema-version-1: backlog  # AMENDED 2026-05-15 (Stephane, /bmad-correct-course): schema anchor flips 9-9a → 9-9c (unified single-shape schema, no oneOf, no v1/v2 branches). Bundled-asset shape changes from "v1 pHash data + schema_version: 1" to "unified per-HUD shape per 9-9c — one `map_config.<hud_version>.json` per HUD version, or one manifest pointing at multiple files (final decision at create-story)." AC1 (bundled file path + schema_version), AC3 (schema reference), AC8 (`validateDetectionConfig` acceptance) all need rewriting at create-story time. Dependencies block updates: replace `Epic 9 Story 9.1` with `Epic 9 Story 9.9c`. See _bmad-output/sprint-change-proposal-2026-05-15.md for full rationale.
  1-14-firestore-rules-coverage-extended: backlog
  1-15-firestore-rules-production-deploy: backlog
  1-16-detection-config-latest-operator-documentation: backlog
  1-17-webhook-idempotency-regression-coverage: backlog
  1-18-vitest-serial-mode-v1-ci-workaround: backlog
  epic-1-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 2 — Reader-App Build Gate & Activation Telemetry Contract
  # ───────────────────────────────────────────────────────────────────
  epic-2: backlog
  2-1-reader-app-ci-gate-implementation: backlog
  2-2-activation-telemetry-wrapper-payload-allowlist: backlog
  2-3-t0-emission-in-auth-service-on-auth-state-change-paid: backlog
  2-4-t1-coach-emission-in-export-pipeline-on-confirmed-dispatch: backlog
  2-5-t1-active-player-emission-in-cinema-mode-screen-on-first-view-mode-toggle: backlog
  2-6-expo-public-auth-bypass-deny-in-release-configs: backlog
  epic-2-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 3 — Six-State Entitlement: State Machine, UI Banners & Lapsed Screen
  # ───────────────────────────────────────────────────────────────────
  epic-3: backlog
  3-1-derive-entitlement-state-pure-function-six-state-regression-tests: backlog
  3-2-entitlement-banner-for-payment-failed: backlog
  3-3-subscription-required-screen-for-lapsed: backlog
  3-4-offline-indicator-for-offline-grace: backlog
  3-5-payment-warning-web-composition: backlog
  3-6-canceling-status-badge-resubscribe-cta: backlog
  3-7-mobile-login-entitlement-gate: backlog
  3-8-mobile-foreground-re-fetch-after-stripe-customer-portal-round-trip: backlog
  3-9-30-day-mmkv-cached-entitlement-day-31-expiry: backlog
  3-10-session-data-preservation-across-lapse-resubscribe: backlog
  epic-3-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 4 — Web Subscribe & Manage Funnel
  # ───────────────────────────────────────────────────────────────────
  epic-4: backlog
  4-1-web-token-bump-accent-hover-a11y-001-contrast-re-verify: backlog
  4-2-og-image-asset-layout-meta: backlog
  4-3-footer-get-help-link-mailto-support: backlog
  4-4-password-reset-form-sign-in-password-reset-query-handler: backlog
  4-5-cancel-dialog-anti-dark-pattern: backlog
  4-6-empty-subscription: backlog
  4-7-web-english-copy-with-fr-verbatim-insertions: backlog
  4-8-web-funnel-analytics-events: backlog
  epic-4-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 5 — Mobile: Card View, Cinema Mode & Manual Clip from Timeline (G1-gated)
  # ───────────────────────────────────────────────────────────────────
  epic-5: backlog
  5-1-card-view-screen-with-adaptive-grid-sort-persistence: backlog
  5-2-card-to-cinema-mode-tap-navigation: backlog
  5-3-cards-timeline-top-bar-toggle-manual-clip-from-timeline: backlog
  5-4-cinema-mode-immersive-review-with-reveal-on-tap-controls: backlog
  5-5-view-mode-toggle-full-minimap-minimap-hud-no-player-swap: backlog
  5-6-default-full-view-for-unknown-map: backlog
  5-7-view-mode-preference-persistence: backlog
  5-8-next-previous-explicit-buttons-in-cinema-mode: backlog
  epic-5-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 6 — Mobile: Clip Creation, Voice & Export-Share (G2 interleave with Epic 7)
  # ───────────────────────────────────────────────────────────────────
  epic-6: backlog
  6-1-30-second-clip-region-with-bracket-handles-5-60s-bounds: backlog
  6-2-manual-clip-from-any-timeline-point: backlog
  6-3-3-slot-voice-annotation-before-during-after: backlog
  6-4-voice-slot-re-record: backlog
  6-5-clip-preview-with-assembled-voice: backlog
  6-6-mobile-hd-encode-tier-selection: backlog
  6-7-os-share-sheet-dispatch: backlog
  6-8-discord-inline-playable-h264-aac-contract-verification: backlog
  6-9-clip-deletion-cascade: backlog
  epic-6-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 7 — Mobile: Auto-save & Crash Recovery (G2 interleave with Epic 6)
  # ───────────────────────────────────────────────────────────────────
  epic-7: backlog
  7-1-silent-auto-save-in-clip-mode: backlog
  7-2-resume-cinema-mode-at-exact-frame: backlog
  7-3-verify-j2-interruption-resume-e2e: backlog
  epic-7-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 8 — Mobile: French i18n, Help & Manual Error Reporting
  # ───────────────────────────────────────────────────────────────────
  epic-8: backlog
  8-1-french-i18n-bundle: backlog
  8-2-error-reporting-mailto-formatter: backlog
  8-3-account-bottom-sheet-aide-section: backlog
  epic-8-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 9 — Tooling: V1 Pipeline Hardening
  # ───────────────────────────────────────────────────────────────────
  epic-9: in-progress
  9-1-schema-version-1-add-to-map-config-writers: cancelled  # CANCELLED 2026-05-15 (Stephane, /bmad-correct-course): absorbed by 9-9a (merged) + reframed forward by 9-9c — the unified `schema_version: 1` is now the config-shape version, not the detection-method version (the original v1/v2 branching anchor is gone). The mechanism Story 9-1 originally specified (`schema_version: 1` in `map_config_generator.py` + `hash_comparator.py` writers + bundled asset + pytest assert) ships via 9-9a's emit path; the semantic anchor shifts under 9-9c. Brownfield Item 7 framing remains tied to schema evolution discipline, just under a different shape. See _bmad-output/sprint-change-proposal-2026-05-15.md.  # Prior entry: ABSORBED BY 9-9a 2026-05-15 (Stephane, /bmad-create-story on 9-9): 9-9a's schema-v2-evolution work requires `schema_version` to exist in v1 emit path so the `oneOf` discriminator on `schema_version` distinguishes v1 from v2 — Story 9-1's ACs (write `schema_version: 1` in `map_config_generator.py` + `hash_comparator.py` + the bundled `apps/mobile/assets/map_config.json` + pytest assert) are naturally covered as part of 9-9a's implementation. Stays `backlog` for traceability; flip to `cancelled` (with "absorbed by 9-9a" rationale) when 9-9a lands, OR repurpose as the v1-emit follow-up if 9-9a's scope narrows. Brownfield Item 7 framing unchanged. Motivation prior to absorption: schema_version: 1 is the v1 baseline so the v2 ROI/HSV-band partition (see 9-9a/9-9b) can land without ambiguity. See _bmad-output/sprint-change-proposal-2026-05-14.md.
  9-2-tool-5-warden-analyzer-real-footage-ac-validation: cancelled  # CANCELLED 2026-05-14 (Stephane, correct-course). Story validated the LEGACY warden_analyzer (gameDetector + mapIdentifier keyed off legacy HUD 1.0 ROIs in config/config.yaml) against real footage at >=95% accuracy. Real footage is now HUD 2.0 — legacy ROIs no longer fire (gameDetector false-negatives, mapIdentifier null). Story 9.8 / Tool 9 (ROI Detection Tester) is the new-HUD analogue and is already DONE. tooling-WARDEN-001 PRD requirement remains but its V1 validation gate is dropped (Story 10.1 V1 launch checklist's "sign-off note from 9.2" line dies with this cancellation). The future re-fingerprinting story (9-9) will produce a working ROI+HSV detector against new-HUD footage; Tool 9 already validates that path per-frame. See _bmad-output/sprint-change-proposal-2026-05-14.md.
  9-3-reference-hash-regression-for-4-awaiting-hash-maps: cancelled  # CANCELLED 2026-05-14 (Stephane, correct-course). Story regenerated reference pHashes for 4 awaiting-hash maps via hash_comparator.py + frame_labeler.py — both legacy-ROI-bound. Beyond the ROI block: the hash-based map identification approach itself (mapIdentifier.ts pHash matcher → map_config.json reference_hash field) is being REPLACED by ROI+HSV-band detection (Tool 8 discovered_zones fragment → config/config.yaml hand-merge → map_config.json v2 ROI/HSV-band schema). Reference-hash regeneration is therefore structurally moot, not just blocked. Work folds into 9-9. PRD tooling-HASH-001/002 framing + architecture's mapIdentifier.ts "pHash matcher" line need a downstream editorial pass (out of this correct-course's scope; flagged in epics-and-stories.md follow-ups). See _bmad-output/sprint-change-proposal-2026-05-14.md.
  9-4-jsonschema-strict-validation-against-map-config-schema: cancelled  # CANCELLED 2026-05-15 (Stephane, /bmad-correct-course): absorbed by 9-9a (merged) + carried forward by 9-9c — the `jsonschema` strict-validation-at-emit-time gate persists on the unified shape (same `_validate_against_schema` mechanism, new target schema). Story 9-4's ACs (jsonschema.validate at emit; clean error + non-zero exit + no file write on failure; pytest at `apps/tooling/tests/test_map_config_schema.py` for `additionalProperties: false` + missing-required-field cases) ship via 9-9a's AC7 + AC10 and continue under 9-9c's rewritten test suite. See _bmad-output/sprint-change-proposal-2026-05-15.md.  # Prior entry: ABSORBED BY 9-9a 2026-05-15 (Stephane, /bmad-create-story on 9-9): 9-9a's emit-time jsonschema strict validation gate covers both v1 and v2 emit paths in `map_config_generator.py` + `hash_comparator.py` — splitting validation across two stories would invite drift between the v1-only validation 9-4 originally targeted and the v1+v2 validation 9-9a needs. Story 9-4's ACs (jsonschema.validate at emit time; clean error + non-zero exit + no file write on failure; pytest at `apps/tooling/tests/test_map_config_schema.py` for additionalProperties: false + missing-required-field cases) are naturally covered by 9-9a's AC7 + AC10. Stays `backlog` for traceability; flip to `cancelled` (with "absorbed by 9-9a" rationale) when 9-9a lands. Prior motivation: detection-method-agnostic infra (validates v1 today; v2 evolution under 9-9a). See _bmad-output/sprint-change-proposal-2026-05-14.md.
  9-5-video-timeline-labeler-tool-6: review
  9-6-overlay-stack-analyzer-tool-7: done  # Tool 7 — Overlay Stack Analyzer. Created 2026-05-12 (added post-planning via the new-HUD initiative; not in the Epic 9 charter, which enumerates 9.1–9.4 only). Consumes Tool 6's output/labeled/v<ver>/<class>/*.png; emits per-(version,class) mean.png + stddev.png (+ optional HSV variance_heatmap.png) + overlay_stacks_summary.json. Headless batch tool, no GUI. Track C; upstream = Story 9.5 (directory contract only, not blocked on 9.5 being done).
  9-7-auto-roi-discoverer-tool-8: done  # Tool 8 — Auto ROI/HSV Discoverer. Created 2026-05-12 (third post-planning addition via the new-HUD initiative; not in the Epic 9 charter, which enumerates 9.1–9.4 only). Interactive Tk review tool: consumes Tool 7's per-(version,class) stats.npz + overlay_stacks_summary.json; suggests/scores/validates game-state ROI candidates over 4 target classes (lobby, score, pooled in_match, kept-for-rejection transition); manual zone/exclusion editing; exports a hand-merge config fragment (output/auto_rois/<version>/discovered_zones.{json,yaml}) + report.json + per-class previews — NEVER auto-edits config/config.yaml. Bundles a Task-1 amendment to Tool 7 (overlay_stack_analyzer.py): always-on circular-Hue HSV Welford stats + per-cell stats.npz side-car + 2 new summary fields (most_stable_hsv, stability_percentiles) — 9.6 deliverable re-touched, 9.6 story status unchanged. Reuses image_inspector.canvas.ImageCanvas, minimap_zone_selector.hsv_editor.HSVEditor + zone_model.zone_fires + the circular-Hue math from app._compute_zone_hsv, frame_labeler.MAP_LABELS; new GameStateValidator (game-state-keyed mean/std separability proxy). No new deps. Track C; upstream = Story 9.6 (done/merged). Downstream = a future game-detector re-fingerprinting story (hand-merge the fragment into config/config.yaml + map_config.json v2 regen) — out of 9.7's scope.
  9-8-roi-detection-tester-tool-9: done  # Tool 9 — Per-frame ROI Detection Tester. Created 2026-05-13 (fourth post-planning addition via the new-HUD initiative; not in the Epic 9 charter, which enumerates 9.1–9.4 only). Headless batch tool (no GUI), single-file shape (tools/roi_detection_tester.py) like Tool 7. Consumes Tool 8's discovered_zones.yaml (`apps/tooling/output/auto_rois/v<ver>/`) + Tool 6's labeled PNG dataset (`apps/tooling/output/labeled/v<ver>/<class>/*.png`); replays every labeled frame through every discovered zone's hue-wrap cv2.inRange band test (reuses auto_roi_discoverer.validator.band_inrange_ratio — no reinvention); aggregates fires into TP/FP/FN/TN per zone and per-class confusion matrices for **both** the 4-way game-state classifier (lobby/in_match/score/transition; folder→ground-truth: lobby/score/transition direct, any MAP_LABELS folder → in_match) and the N-way map-ID classifier (per-map zones; only evaluated on MAP_LABELS folders). Per-class score = fires_count/n_zones; argmax above threshold (default 0.5) → predicted class, below → "unknown". Output `apps/tooling/output/roi_detection_tests/v<ver>/<timestamp>/{report.json, summary.md, [frame_predictions.csv]}` (timestamped subdirs — re-runs don't overwrite). Closes the per-frame validation gap Tool 8 explicitly deferred (auto_roi_discoverer/validator.py:18-20 + auto_roi_discoverer.md's "What's a proxy" section). Reuses auto_roi_discoverer.model.Rect/HsvBand + frame_labeler.MAP_LABELS + overlay_stack_analyzer._default_input_dir; no new deps (numpy/opencv-python/pyyaml/stdlib present). Tool 9 still NEVER writes config/config.yaml (the hand-merge + map_config.json v2 regen remain the future re-fingerprinting story). Track C; upstream = Story 9.7 (review — branch off main once 9.7 merges; off tool-8-auto-roi-discoverer otherwise + rebase). Downstream = the future game-detector re-fingerprinting story — out of 9.8's scope.
  9-9-re-fingerprint-config-for-hud-2-0: cancelled  # SPLIT 2026-05-15 via /bmad-create-story (Stephane). Original single 9-9 stub flagged "larger than one sprint — needs splitting at create-story time" (2026-05-14 correct-course). Create-story split it into 9-9a (schema+code, ready-for-dev), 9-9b (iterative hand-merge, backlog), 9-10 (docs editorial, backlog). V1 posture: post-V1 (V1 ships with legacy v1 map_config.json untouched; consumers won't fire on HUD 2.0 footage; mobile TS consumer rewrite is a separate post-V1 story TBD). Mobile scope confirmed out of all three sub-stories — 9-9a ships v1 emit unchanged so mobile consumers don't break. See sprint-status header `last_updated` + _bmad-output/epics-and-stories.md "Story 9.9" split section for full rationale. Original spec preserved in epics-and-stories.md for traceability.
  9-9a-schema-v2-and-map-config-generator: review  # SUPERSEDED 2026-05-15 (Stephane, /bmad-correct-course): the `oneOf` v1/v2 schema design and the dual-emit-path in `map_config_emitter.py` are unified into a single shape under new Story 9-9c. Reasoning: v1 branch turned out to be a phantom — no consumer exists. The merged work is real history (`schema_version` field exists; `map_config_emitter.py` exists in the renamed-from-`map_config_generator.py` form; `jsonschema` validation gate works); 9-9c rewrites the schema and the emitter on top of that baseline. Status stays `review` until the standard Two-PR follow-up flips it `done` — don't conflate with 9-9c. Story file `_bmad-output/implementation-artifacts/9-9a-schema-v2-and-map-config-generator.md` will receive a Scope Adjustment #3 block recording this. See _bmad-output/sprint-change-proposal-2026-05-15.md.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-create-story split-derived from 9-9). Code path: `contracts/map-config.schema.json` v2 additions (`schema_version` integer field [enum 1,2] + ROI/HSV-band-keyed `maps` shape via `oneOf` discriminator on `schema_version`, preserving v1 pHash path side-by-side; new `game_state_zones: Record<state_name, ZoneSpec[]>` top-level field for v2; `additionalProperties: false` at all levels); `apps/tooling/tools/map_config_generator.py` + `hash_comparator.py` emit v2 when input `apps/tooling/config/config.yaml` carries ROI/HSV-band content (detect via presence of `game_state_zones` block OR ROI/HSV data in `minimap_identification`), fall back to v1 pHash emit otherwise; jsonschema strict validation at emit time (folds Story 9-4's AC); Zod regen via `pnpm --filter @warden/contracts build`. Naturally absorbs Story 9-1's ACs (schema_version: 1 in v1 emit path). NOT a breaking change for mobile consumers — 9-9a does NOT ship v2 to mobile; the bundled `apps/mobile/assets/map_config.json` is regenerated with v1 emit (same pHash data + schema_version: 1 field added). Out-of-V1-scope still (per parent 9-9 stub) BUT the schema+code work is V1-safe: shipping 9-9a in V1 would only add schema_version: 1 to the bundled v1 map_config.json (additive, non-breaking). Downstream of 9-7 (done — Tool 8 fragment shape `apps/tooling/tools/auto_roi_discoverer/export.py:72-91` is the v2 zone-dict reference) + 9-8 (done — Tool 9 validator). Upstream of 9-9b. Track C (tooling).
  9-9b-iterative-zone-population-for-shipping-configs: ready-for-dev  # CREATE-STORY 2026-05-15 (Stephane, /bmad-create-story on 9-9b): flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-9b-iterative-zone-population-for-shipping-configs.md` finalized with full developer context: 12 ACs (hard-dependency landed gate; labeled dataset coverage ≥ thresholds per HUD; per-HUD `manifest.json` written with score-screen-duration calibration procedure; zone fragments populated via `zone_picker` mode-by-mode; emitted `map_config.v<hud_version>.json` validates against unified schema; per-classifier accuracy floors measured via Tool 9 refit — HUD-version ≥ 99% / in-match ≥ 97% / per-map ID ≥ 95% anchored to PRD REL-006; iteration log per HUD version with measurement-and-adjust loop capped at 8 passes; end-to-end video-pipeline validation via `video_test` 9.13 [deferrable if 9.13 not done]; AC9 zone-fragment commit-location decision [Option A `.gitignore` exception (default) vs Option B committed config dir]; no-code-touch invariant + test suite green gate; sprint-status lifecycle flip + Two-PR follow-up per HUD-version cohort), 7 Tasks (pre-flight verification → per-HUD manifest authoring → initial zone-picking pass → measurement-and-adjust iteration loop → end-to-end real-video validation → story closure → PR + sprint-status flips), Dev Notes (strategic context — operations/data-production story not code; key code patterns to reuse via 9.9c contract surface; anti-patterns/disasters — no .py touch, no schema touch, no hand-edits of fragments or assembled config, held-out-cohort discipline, 8-pass ceiling; file structure tree; library/framework requirements — no new deps; testing standards — Tool 9 refit IS the test, no synthetic data, held-out cohort discipline, reproducibility via timestamped report subdirs; sprint-fit + dependencies — hard upstream 9-9c/9-11/9-12/9-14, recommended 9-13, downstream 1-13 + 9-10; project structure notes — naming + AC9 decision + per-HUD partitioning + slug normalization + HUD enum extension; references). Dev cycle blocked until 9-9c (review) → 9-11 → 9-12 / 9-14 (and ideally 9-13) all land on `main`. Multi-sprint posture: ~1.5 weeks per HUD version × default scope (v2 only). All 12 ACs held `[ ]` per [[feedback_ac_checkbox_tighten]] — every AC's endpoint depends on either upstream story completion, empirical measurement not yet performed, or post-merge admin. epic-9 stays `in-progress`. No code touched in this skill run.  # Prior entry: RENAMED + MECHANISM REWRITE 2026-05-15 (Stephane, /bmad-correct-course): previous key was `9-9b-tool-8-fragment-hand-merge-and-config-regen`. Goal at the deliverable level is unchanged (a populated `map_config.<hud_version>.json` per shipping HUD version + documented per-class accuracy floors), but the mechanism is rewritten: no more `config.yaml` editing or `discovered_zones.yaml` hand-merge — those tools retire under 9-11. New mechanism: iteratively run zone_picker (9-12) → map_config_emitter (per 9-9c) → video_test (9-13) and/or roi_detection_tester (refit per 9-14) → adjust zones → repeat until per-class accuracy thresholds are met. Multi-sprint manual work. Out-of-V1-scope. Dependencies block updates: replace `9-9a` with `9-9c, 9-12, 9-13, 9-14`. See _bmad-output/sprint-change-proposal-2026-05-15.md.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-create-story split-derived from 9-9). Iterative manual hand-merge: Tool 8 discover → Tool 9 measure → hand-merge `apps/tooling/output/auto_rois/v<ver>/discovered_zones.yaml` fragment into `apps/tooling/config/config.yaml` (replacing legacy `minimap_identification:87-907` pHash zone block + adding game-state cascade zones — game-state classes are empty in current v2.0 fragment per the live yaml; per-map classes are populated for 14 maps) → regenerate `apps/mobile/assets/map_config.json` v2 via 9-9a's updated generator → re-run Tool 9 against the regenerated config → document per-class accuracy floors. Multi-sprint manual work. Out-of-V1-scope. Downstream of 9-9a (needs the v2 emit path); upstream of 9-10. Track C (tooling).
  9-10-prd-architecture-editorial-pass-roi-hsv-pivot: ready-for-dev  # CREATE-STORY 2026-05-16 (Stephane, /bmad-create-story on 9.10): flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-10-prd-architecture-editorial-pass-roi-hsv-pivot.md` finalized: 11 ACs (AC0 rollout decision; AC1 scope-boundary attestation gate; AC2/AC3/AC5 Cohort-A prose scrub of named targets + Epic 9 charter Goal line; AC4 PRD-V1-tooling-treatment decision [default = annotate, not destructive reframe — V1 genuinely ships pHash]; AC6 Cohort-B accuracy-floor citations GATED on 9.9b; AC7 no-code-touched invariant; AC8 cross-ref integrity; AC9 sprint-status lifecycle [HELD]; AC10 Single-PR + Two-PR follow-up [HELD]), 8 Tasks. Two-cohort structure: Cohort A (prose scrub) UNBLOCKED by 9.9c `done` (`9b9d4af`); Cohort B (accuracy citations) GATED on 9.9b measured floors → AC0 default (Option A) holds the whole story until 9.9b `done` to avoid editing the same paragraphs twice. KEY FINDINGS recorded in Editorial Target Manifest: architecture.md has ZERO `oneOf`/v1-v2 prose (scope-expansion's architecture target = verify-and-record-negative); ~60 stale-prose hits but the majority are inside deliberately-FROZEN blocks (cancelled 9.1/9.2/9.3/9.4, SUPERSEDED 9.9a, preserved original-9.9-stub per `sprint-status.yaml:183`, architecture.md Brownfield-Item-7-as-V1-shipped) — editing them destroys the pivot audit trail; AC1 makes the FROZEN boundary non-negotiable. Dev-cycle kickoff held until 9.9b reports. All ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; post-merge ACs tagged [HELD]. Two-PR + local `--no-ff` delivery per [[feedback_two_pr_docs_execution]] + 9.9c precedent. epic-9 stays `in-progress`. No code touched in this skill run.  # Prior entry: SCOPE EXPANDED 2026-05-15 (Stephane, /bmad-correct-course): in addition to the original editorial targets (`tooling-HASH-001/002`, `mapIdentifier.ts` "pHash matcher" line, `mobile-AUTO-SLICE-002/003` traceability), also scrub any 9-9a-era prose referencing the now-superseded `oneOf` / v1-v2 branching framing. Search targets: `epics-and-stories.md` (the 9-9 stub area + Epic 9 charter amendment block), `architecture.md` (any schema-versioning references), `prd.md` (no expected hits). Still anchored to 9-9b's regenerated configs + measured accuracy floors. Still small standalone. Still post-V1. See _bmad-output/sprint-change-proposal-2026-05-15.md.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-create-story split-derived from 9-9 AC4). Pure docs editorial: PRD `tooling-HASH-001/002` ([prd.md:955-956](prd.md#L955-L956)) + architecture `mapIdentifier.ts` "pHash matcher against map_config" line ([architecture.md:1505](architecture.md#L1505)) + `mobile-AUTO-SLICE-002/003` traceability ([architecture.md:1691](architecture.md#L1691)) wording updated to reflect the ROI+HSV pivot. Small standalone story; anchored to 9-9b's regenerated config + measured accuracy floors. Out-of-V1-scope. Downstream of 9-9b. Track C (tooling-docs).
  9-9c-schema-unification: done  # MERGE 2026-05-16 (Stephane, during /bmad-dev-story 9.9b run): flipped `review → done`. Scope Adjustment #5 reverses #4's consolidated-Epic-9-PR deferral — 9.9c merged independently to `main` to unblock the dep chain (9.9c → 9.11 → 9.12/9.14 → 9.13 → 9.9b). `gh` unauthenticatable non-interactively → delivered as a local `git merge --no-ff tooling-refactor → main` (merge commit `9b9d4af`, pushed `890fa01..9b9d4af`); single-delivery shape + Two-PR follow-up (branch `story-9-9c-postmerge`) preserved. Pre-merge gates: apps/tooling pytest 168 passed; `pnpm --filter tooling test` 168 passed. AC11 + AC12 + Task 7 (+ sub-boxes) flipped `[x]`; story Status `done`. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (still blocked on 9.11/9.12/9.13/9.14 — none exist yet). See story Change Log Scope Adjustment #5.  # Prior entry: DEV-STORY 2026-05-15 (Stephane, /bmad-dev-story on 9-9c, executed by Amelia): flipped `ready-for-dev → in-progress → review`. Tasks 1–6 complete (Task 7 in-flight: commit + push + PR). Deliverable: `contracts/map-config.schema.json` rewritten as a single unified shape (no `oneOf`; new `$defs.Rect` + `$defs.Zone` + `$defs.MapEntry`; `weight_override: number|null`); `apps/tooling/tools/map_config_emitter.py` retargeted to fragment-driven input with `--zones-dir` CLI (`_load_fragments` + `_assemble_output` added; v1/v2 detection + dual emit branches deleted; `_validate_against_schema` + `_format_validation_error` preserved verbatim from 9.9a); `apps/tooling/wardentooling.py` `flow_tool3` retargeted to `--zones-dir` with `apps/tooling/output/map_configs/` string-literal output default; `apps/tooling/tests/test_map_config_emitter.py` rewritten (23 cases: `TestLoadFragments` ×4, `TestAssembleOutput` ×3, `TestJsonschemaValidationGate` ×11, `TestEmit` ×5; old `TestDetectSchemaVersion` / `TestBuildV1Output` / `TestBuildV2Output` / `TestLiveConfigSmoke` deleted). Full apps/tooling pytest suite 154 green (net delta −8 from 162: 17 deleted cases + 14 adapted-replaced-by-23-new; the AC8 ~+3 estimate was off because more old cases were folded into the rewrite than estimated — all four new buckets still meet/exceed AC8's ≥4/≥3/≥8/≥5 floors). [SUPERSEDED by the 2026-05-15 first-pass code-review round: target file 23 → 37 cases, full apps/tooling suite 154 → 168 green (final delta +6 vs the 162 baseline); see story Change Log + Review Findings — Second Pass 2026-05-16.] Zod regen produces a single `z.object({...}).strict()` (`.superRefine` block wraps `patternProperties` for `maps` slug regex, NOT `oneOf` — AC6 satisfied). Cross-workspace typecheck baseline preserved: web 16 errors + mobile 1 error are categorically identical to the 9.9a-documented baseline (Stripe API version pin / Zod 4 vs react-hook-form / Firebase v12 `getReactNativePersistence` / test spreads / tuple-index / implicit any); `git grep -F MapConfigSchema apps/` returns 0 hits + `git grep "from '@warden/contracts'" apps/ packages/` returns 0 hits, confirming consumer-invisible Zod shape change; formal stash-and-compare audit skipped (no consumer exists; analytical chain dispositive). AC1–AC10 flipped `[x]`. AC11 + AC12 held `[ ]` per [[feedback_ac_checkbox_tighten]] (post-merge sprint-status flips + PR open). **Scope adjustment to AC12:** work landed on the existing `tooling-refactor` branch (which already carries the `95cdac3` correct-course commit + the uncommitted story file + the ready-for-dev flip) instead of a fresh `story-9-9c-schema-unification` branch off `main`. Single-PR delivery preserved (`tooling-refactor → main` bundles `95cdac3` with the 9-9c implementation). Two-PR follow-up pattern preserved for the `review → done` flip post-merge. epic-9 stays `in-progress`. Stories 9.1 + 9.4 already at `cancelled` from the 2026-05-15 correct-course — no flips required here. No new dependencies. No code regressions.  # Prior entry: DEV-STORY 2026-05-15 (Stephane, /bmad-dev-story on 9-9c, executed by Amelia): flipped `ready-for-dev → in-progress`. Branch deviation from AC12: per Stephane's direction at dev-story kickoff, work lands on the existing `tooling-refactor` branch (which already carries the 95cdac3 correct-course commit + the uncommitted story file + the ready-for-dev flip) instead of cutting a fresh `story-9-9c-schema-unification` off `main`. PR will go `tooling-refactor → main` and will bundle 95cdac3 (already on the branch) with the 9-9c implementation. Recorded as a Scope Adjustment in the story's Change Log. Note: 9.9a is on `main` already (commit 890fa01), so all the 9.9a baseline files exist on `main` to land 9-9c against once the PR ships.  # Prior entry: CREATE-STORY 2026-05-15 (Stephane, /bmad-create-story on 9-9c): flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-9c-schema-unification.md` finalized with full developer context: 12 ACs (single unified schema shape with no `oneOf`; emitter retargeted to read disk-resident fragments at `apps/tooling/output/zones/v<hud_version>/{manifest,hud_version_detection,in_match_detection,minimap_identification}.json` and emit `map_config.<hud_version>.json` per-HUD-version; `jsonschema` strict-validation gate preserved verbatim from 9-9a; `wardentooling.py` `flow_tool3` retargeted to `--zones-dir`; pytest rewritten with synthetic-fragment fixtures — old v1/v2 branch test buckets deleted; Zod regen as a single `z.object` and cross-typecheck baseline preserved; no new deps; sprint-status `review → done` flip bundled into the post-merge follow-up per Two-PR pattern; 9-1 and 9-4 are already at `cancelled` from the 2026-05-15 correct-course, no flips required), 7 Tasks (schema rewrite → emitter input layer + assembler → emit pipeline + CLI → wardentooling.py update → test rewrite → Zod regen + cross-typecheck audit → PR + sprint-status flips), 4 Dev Notes sections (strategic context, reuse patterns, anti-patterns, file structure + dependencies + sprint-fit + project notes + references). AC11 (sprint-status lifecycle flip) and AC12 (Single-PR + Two-PR follow-up) held `[ ]` per [[feedback_ac_checkbox_tighten]] + [[feedback_two_pr_docs_execution]]. epic-9 stays `in-progress`. No code touched in this skill run.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-correct-course). Code path: `contracts/map-config.schema.json` rewritten as a single unified shape (no `oneOf`, no v1/v2 branches). Top-level required fields: `schema_version: integer enum [1]` (config-shape version, not detection-method version), `reference_resolution`, `hud_version: string enum ["v1","v2",...]`, `score_screen_duration_ms: integer >= 0` (timing offset for score-screen extraction after `in_match` ends; lives in the schema because it's per-HUD-version), `hud_version_detection: Zone[]`, `in_match_detection: Zone[]` (binary in-match-or-not — no 4-class game-state cascade; score is timing-derived, lobby/transition not differentiated), `minimap_identification: {id, identification_threshold, roi, maps: {<slug>: {zones: Zone[]}}}`. One `Zone` shape via `$defs`: `{id, x, y, width, height, hsv, min_ratio, weight, weight_override}`. `additionalProperties: false` at every level. `apps/tooling/tools/map_config_emitter.py` retargeted: single emit path (no `_detect_schema_version`, no `_build_v2_output` branch, no v1-coercion). `jsonschema.validate()` gate preserved end-to-end (same `_validate_against_schema` mechanism, new target). Zod regen via `pnpm --filter @warden/contracts build` → single object type (no `discriminatedUnion`). Pytest rewritten: `test_map_config_emitter.py` covers valid emit + missing `hud_version_detection` / missing `in_match_detection` / missing `score_screen_duration_ms` / HSV out-of-range / negative score-duration / extra unknown fields. Cross-workspace typecheck gate (`pnpm --filter web typecheck` + `pnpm --filter mobile typecheck`) baseline preserved (no new regressions; pre-existing baseline-failure set from 9-9a still applies). Supersedes 9-9a's schema; absorbs 9-1 + 9-4 forward. Dependencies: 9-9a (done; provides emitter baseline). Sprint fit: fits-in-one-sprint (~2 days). Track C. Out-of-V1-scope at the deliverable level (no mobile consumer yet); V1-safe at the code level (the new schema only ships once Story 1-13 picks it up). See _bmad-output/sprint-change-proposal-2026-05-15.md for full spec.
  9-11-retire-legacy-tooling: done  # MERGE 2026-05-16 (Stephane, /bmad-dev-story 9.11 post-merge follow-up, Amelia): flipped `review → done`. Main delivery merged to `main` via local `git merge --no-ff story-9-11-retire-legacy-tooling` → merge commit `aca0906` (41 files, +555/−9720). Post-merge regression on `main`: apps/tooling pytest 108 passed; `pnpm --filter tooling test` 108 passed. AC1–AC9 + Tasks 1–9 `[x]`; story Status `done`. Two-PR follow-up (`story-9-11-postmerge`) delivered as a second local `--no-ff` merge. epic-9 stays `in-progress`; 9.12/9.13/9.14/9.9b unchanged (still dep-blocked). Dep chain 9.9c → 9.11 now satisfied; 9.12/9.14 unblocked on the "no about-to-die imports" hazard.  # Prior entry: DEV-STORY 2026-05-16 (Stephane, /bmad-dev-story 9.11, executed by Amelia): flipped `ready-for-dev → in-progress → review`. Tasks 1–8 complete; AC1–AC7 `[x]`. Relocated shared primitives to `apps/tooling/tools/common/{labels,zones,labeled_dataset}.py` (verbatim except the sanctioned `_default_input_dir` depth-fix, guarded by new `test_common_labeled_dataset.py`); repointed Tool 6 + Tool 9 + Tool 9's test; scrubbed all stale retired-module prose (tree-wide AC3 grep = 0); deleted 9 retire-list entries + 2 `.md` + 2 retired test files + orphaned `astera_expected.json`; de-registered Tools 1/2/5/7/8 + 2 Dev flows from `wardentooling.py`; deleted `config.yaml` `minimap_identification` block (lines 87–912, YAML still parses). apps/tooling pytest 168 → 108 (−63 deleted [30+33] +3 mandated depth-fix guard); `pnpm --filter tooling test` 108 green; smoke OK. AC7 orphan report: `utils/config.py`+`config.yaml` now FULLY ORPHANED → flagged candidate follow-up retirement, NOT deleted (don't-widen-scope). Delivered as local `git merge --no-ff story-9-11-retire-legacy-tooling → main` (9.9c Scope-Adjustment-#5 precedent). AC8/AC9 + Task 9 `[ ]`/`[HELD]` — `review → done` + post-merge box-flips land via `story-9-11-postmerge`. epic-9 stays `in-progress`. 9.12/9.13/9.14/9.9b unchanged (still dep-blocked).  # Prior entry: DEV-STORY 2026-05-16 (flipped `ready-for-dev → in-progress`; implementation in flight).  # Prior entry: CREATE-STORY 2026-05-16 (Stephane, /bmad-create-story on 9.11): flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-11-retire-legacy-tooling.md` finalized: 9 ACs (retire-list deleted; shared primitives relocated to `tools/common/` BEFORE delete; survivor + surviving-test imports repointed; `wardentooling.py` fully de-registered; 2 retired test files deleted + suite green; emitter survivor confirmed clean post-9.9c + `pnpm --filter tooling test` green; config.yaml `minimap_identification` decision [HELD D2]; sprint-status lifecycle [HELD]; Single-PR + Two-PR follow-up [HELD]), 9 Tasks (pre-flight + dep gate → relocate primitives → repoint survivors → delete retire-list + .md → strip wardentooling → delete test files → config.yaml decision + orphan report → full validation → PR + sprint-status flips). KEY FINDING: import-graph audit overturned the stub's "no cross-package imports / ~1-day mechanical" framing — surviving Tool 6 `video_timeline_labeler.py` + Tool 9 `roi_detection_tester.py` (+ `test_roi_detection_tester.py`) import retire-list modules; 9.11 must relocate `MAP_LABELS`/`LABEL_DISPLAY`/`Rect`/`HsvBand`/`TARGET_CLASSES`/`band_inrange_ratio`/`_default_input_dir` to `apps/tooling/tools/common/` + repoint before deleting (else red suite; Tool 6 has no refit story, Tool 9's refit 9.14 lands after 9.11). Re-estimated ~1.5-2 days, still fits-in-one-sprint. Upstream 9.9c `done` (`9b9d4af`) — emitter already decoupled, AC6 verify-only. Verified test reality: only `test_auto_roi_discoverer.py` + `test_overlay_stack_analyzer.py` exist for retire-list (other 7 have none). All ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; post-merge/decision-pending tagged `[HELD]`. Two-PR + local `--no-ff` delivery per [[feedback_two_pr_docs_execution]] + 9.9c precedent. epic-9 stays `in-progress`. No code touched in this skill run.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-correct-course). One atomic deletion PR removing 9 retired tool files/packages + their `wardentooling.py` registrations + their `.md` docs + their tests. Retire list: `apps/tooling/tools/auto_roi_discoverer/` (entire package — 9 modules + sibling .md; auto-suggest layer didn't produce good-enough zones in practice, manual picker wins, git history preserves the code); `apps/tooling/tools/minimap_zone_selector/` (entire package — predecessor of Tool 8; same job; folds into 9-12 zone_picker); `apps/tooling/tools/overlay_stack_analyzer.py` + `.md` (variance/heatmap signal folds into 9-12 zone_picker as an in-tool preprocessing helper); `apps/tooling/tools/game_detector.py` (Tool 1 — black-screen detection; replaced by `in_match_detection` zones); `apps/tooling/tools/black_screen_detector.py` (same era); `apps/tooling/tools/bsd_roi_debugger.py` (debugger for the above; obsolete); `apps/tooling/tools/frame_labeler.py` (Tool 2 — Tool 6 `video_timeline_labeler.py` supersedes); `apps/tooling/tools/warden_analyzer.py` (Tool 5 — Story 9-2 cancelled; HUD 2.0 footage breaks its legacy ROIs); `apps/tooling/tools/points_state_detector.py` (legacy dead code). Plus: `apps/tooling/wardentooling.py` registrations stripped (flow-functions + `_TOOL_MAP` entries + `choices_main` branches + `menu_main` branches + `_reprompt_source` branches for each retired module). Plus: `apps/tooling/tests/test_*.py` files for retired tools deleted (verify each before delete). Plus: `apps/tooling/config/config.yaml` legacy `minimap_identification:87-907` block — decision at create-story (recommendation: delete it since unreferenced once 9-9c emitter doesn't read from config.yaml). Sequencing: ideally lands after 9-9c (so `map_config_emitter.py` no longer transitively imports retired helpers) OR before 9-12/9-13/9-14 (so new tools don't accidentally import about-to-die modules). Sprint fit: fits-in-one-sprint (~1 day mechanical). Track C. Pre-production posture (no backward-compat) makes this safe. See _bmad-output/sprint-change-proposal-2026-05-15.md.
  9-12-unified-zone-picker: done  # MERGE 2026-05-17 (Stephane, /bmad-dev-story 9.12 post-merge Two-PR follow-up, Amelia): flipped `review → done`. AC13 post-merge gate confirmed on `main` HEAD (`54724ed`) — apps/tooling pytest 132 passed, 0 regressions to the Story-9.11 108 baseline. AC13/AC14/AC15 + Task 8 `[x]`; story Status `done`. Delivered as a second local `git merge --no-ff story-9-12-postmerge → main`. The Epic 9 dep chain step 9.9c → 9.11 → 9.12 is satisfied — zone_picker (Tool 10) is on `main`. epic-9 stays `in-progress`; 9.9b stays `ready-for-dev` (still blocked on 9.13/9.14, both `backlog`).  # Prior entry: DEV-STORY 2026-05-17 (Stephane, /bmad-dev-story, Amelia): flipped `in-progress → review`. Tasks 1–7 + AC1–AC12 `[x]`; implemented package `apps/tooling/tools/zone_picker/` (__init__/__main__/fragments[Tk-free]/variance[Tk-free]/modes/app[Tk]) + `tests/test_zone_picker.py` (24 pure-logic cases) + `wardentooling.py` Tool 10 registration. Welford/circular-Hue ported verbatim from `overlay_stack_analyzer.py@ba6b326`; `derive_band_for_rect`/`circular_*_cv` from `auto_roi_discoverer/discoverer.py@0c1c656^`. `ImageCanvas`/`ROIMode`/`HSVFilterMode` reused via adapter-shim (no coordinate/mask-math fork). VideoPlayer verified-negative (PNG input). serialize_zone clamps to schema (h_tol→180, bool weight_override→null); emitter does no coercion. apps/tooling pytest 108 → **132** (+24, 0 regressions); `pnpm --filter tooling test` 132 parity; `python -m tools.zone_picker --help` + `import wardentooling` smoke clean. Delivered via local `git merge --no-ff story-9-12-unified-zone-picker → main`. AC13/14/15 + Task 8 `[ ] [HELD]` — `review → done` + post-merge box-flips land via `story-9-12-postmerge` Two-PR follow-up per [[feedback_two_pr_docs_execution]] + 9.9c/9.11 precedent. epic-9 stays `in-progress`. 9.9b unchanged (still blocked on 9.13/9.14).  # Prior entry: DEV-STORY 2026-05-16 (Stephane, /bmad-dev-story, Amelia): flipped `ready-for-dev → in-progress`. Task 1 pre-flight green (deps on main, pytest baseline 108, contract surface read & confirmed). Implementation in flight.  # Prior entry: CREATE-STORY 2026-05-16 (Stephane, /bmad-create-story on 9.12, Amelia): flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-12-unified-zone-picker.md` finalized: 15 ACs (package not single-file [AC1]; reuse `ImageCanvas`+`HSVFilterMode`+`ROIMode` — `HSVEditor` stale/dead [AC2]; PNG-seq nav in-tool, `VideoPlayer` verified-N/A [AC3]; 3 modes→declared fragment targets [AC4]; ported Welford/circular-Hue variance helper [AC5]; recovered `derive_band_for_rect` band auto-seed [AC6]; in-tool fire-ratio feedback fenced [AC7]; per-map `weight`+`weight_override` number|null [AC8]; merge-safe load-all/write-all-4-fragments anti-clobber + MAP_LABELS ordering [AC9]; unchanged-emitter round-trip green [AC10]; pure-logic-only pytest no-Tk [AC11]; wardentooling reg as Tool 10 [AC12]; [HELD] suite-green/sprint-status/Two-PR AC13–15), 8 Tasks, exhaustive Dev Notes (strategic context, package decision, exact reuse-symbol map + 5 disaster-prevention items, verbatim fragment+Zone contract from live `main`, verbatim recovered variance/band-seed algorithm to port, lib reqs no-new-deps, 9.11/9.9c previous-story intel, sprint-fit + dep gate, project structure, references). Dependencies SATISFIED: 9.9c `done` (`9b9d4af`) + 9.11 `done` (`aca0906`) on `main` — 9.12 fully unblocked. Sprint fit fits-in-one-sprint high end (~4–5 days). All 15 ACs `[ ]` per [[feedback_ac_checkbox_tighten]]; AC13/14/15 + Task 8 `[HELD]`. Two-PR + local `--no-ff` per [[feedback_two_pr_docs_execution]] + 9.9c/9.11 precedent. epic-9 stays `in-progress`. No code touched in this skill run.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-correct-course). New tool: `apps/tooling/tools/zone_picker.py` (single-file OR single-package — decide at create-story based on size). Interactive Tk picker collapsing workflow steps #2/#3/#4 into one tool with 3 selectable modes: (1) HUD-version mode — input = cross-version PNGs (any class from each `labeled/v<hud>/`); output target = `hud_version_detection: Zone[]` in the v<hud> fragment; (2) In-match mode — input = positive class pooled from any `labeled/v<hud>/<MAP_LABELS_slug>/`, negative class pooled from `labeled/v<hud>/{lobby,score,transition}/`; output target = `in_match_detection: Zone[]`; (3) Per-map mode — input = `labeled/v<hud>/<map_slug>/`; output target = `minimap_identification.maps.<map_slug>.zones: Zone[]` with `weight` + `weight_override` editing for 100%-accurate per-map ID. Reuses `apps/tooling/tools/image_inspector/` primitives (ImageCanvas + HSVEditor — no GUI reinvention). Reuses `apps/tooling/tools/common/video_player.py` if frame-stepping is required. Folds in `overlay_stack_analyzer`'s variance/heatmap signal as an in-tool preprocessing helper (compute mean + stddev + Hue-stability heatmap on the fly per labeled class — no separate Tool 7 needed). Writes zone fragments to `apps/tooling/output/zones/v<hud_version>/{hud_version_detection,in_match_detection,minimap_identification}.json` (final layout at create-story) that `map_config_emitter.py` consumes. Pytest for pure logic (fragment serialization + variance helper); no Tk GUI testing per Tool 6/8 precedent. `wardentooling.py` registers zone_picker as a top-level menu entry. Dependencies: 9-9c (target schema shape); 9-11 (retires the predecessors this tool replaces — sequencing 9-9c → 9-11 → 9-12 safest). Sprint fit: fits-in-one-sprint, possibly two (GUI complexity: Tk modes, HSV sliders, variance preprocessing, per-map weight controls — likely 3-5 days focused work; estimate at create-story). Track C. See _bmad-output/sprint-change-proposal-2026-05-15.md.
  9-13-video-detection-tester: backlog  # NEW 2026-05-15 (Stephane, /bmad-correct-course). New tool: `apps/tooling/tools/video_test.py` (single-file shape like Tool 7/9 precedent — pure helpers + thin `main(argv) -> int`). Headless tool that consumes an emitted `map_config.<hud_version>.json` and runs the full detection pipeline against an actual video. CLI: `python tools/video_test.py <video.mp4> [--config <map_config.json>] [--output <results.json>]`. Pipeline stages: (a) i-frame extraction via `cv2.VideoCapture` + configurable stride (default ~1 i-frame/sec); (b) once-per-session HUD-version classification via `hud_version_detection` zones → selects which `map_config.<hud>.json` applies (if multiple bundled); held constant for the run; (c) per-i-frame binary `in_match` classification via `in_match_detection` zones (argmax_above_threshold over zone-fire ratio, same mechanism as Tool 9); (d) state machine — on rising edge → enter `in_match`; on falling edge → enter `score_screen` for `score_screen_duration_ms` (frames in this window flagged for score-screen extraction); after the score window → `not_in_match` (lobby/transition merged; not differentiated) until next rising edge; (e) for frames inside an `in_match` span → map-ID classification via `minimap_identification` (weighted); hold the most-confident map-ID across the span as canonical. Output `results.json` shape: top-level `hud_version` + ordered list of `{frame_idx, timestamp_ms, state ∈ {in_match, score_screen, not_in_match}}` + `matches: [{start_frame, end_frame, map_id, confidence}, ...]`. Reuses Tool 9's per-frame zone-fire logic (no reinvention). Pytest for pure logic (pipeline stage helpers, state machine, i-frame stride math) + manual smoke test against ≥1 real EVA capture. `wardentooling.py` registers video_test. Dependencies: 9-9c (target schema + score_screen_duration_ms field); ideally 9-12 (populated zone configs to test against) + 9-14 (HUD-version classifier already exercised on labeled PNGs). Sprint fit: fits-in-one-sprint (~2-3 days). Track C. Closes workflow step #6 (end-to-end video pipeline). See _bmad-output/sprint-change-proposal-2026-05-15.md.
  9-14-roi-detection-tester-refit-for-unified-schema: review  # DEV-STORY 2026-05-17 (Stephane, /bmad-dev-story on 9.14, Amelia): flipped `ready-for-dev → in-progress → review`. Tasks 1–6 + AC0–AC11 `[x]`. `roi_detection_tester.py` rewritten for the unified schema (loader → 3 classifiers [HUD-version NEW + binary in_match REPLACES the 4-way cascade + weighted per-map] → 3-section report.json/summary.md/csv; `ZoneSpec` id+weight+`name`-backcompat-alias; `_normalize_hud`; `_default_config_path`; `--config`/threshold CLI; cp1252-safe I/O). `test_roi_detection_tester.py` refit 33→52 (game-state-cascade deleted; synthetic JSON only). `wardentooling.flow_tool9` rewired to `--config` picker + 3 threshold prompts (Tool 9 numbering unchanged). Full suite **194 passed** on `uv run pytest -q` AND `pnpm --filter tooling test` (= 132 Story-9.12 baseline + 43 Story-9.13-uncommitted + 19 this story; 0 regressions to the 175 pre-work baseline). 9.13's contract-pin + `video_test` imports green against the refit via the `name` alias (collision resolution). AC12 smokes clean (--help, wardentooling import, empty-zones run). AC0 decisions: (0a) weighted per-map aggregate; (0b) `_normalize_hud` lowercase+strip-trailing-`.0`. AC12/13/14 + Task 7 `[ ] [HELD]` — `review → done` + post-merge box-flips + local `--no-ff` delivery (commit-boundary-separated from 9.13's uncommitted work) land via `story-9-14-postmerge` Two-PR follow-up per [[feedback_two_pr_docs_execution]] + 9.9c/9.11/9.12 precedent. epic-9 stays `in-progress`. 9.9b stays `ready-for-dev` (after 9.14 lands, blocked only on 9.13).  # Prior entry: DEV-STORY 2026-05-17 (Stephane, /bmad-dev-story on 9.14, Amelia): flipped `ready-for-dev → in-progress`. Pre-flight gate green; baseline `uv run pytest -q` = 175 (9.13 delivered-but-uncommitted on `main`, +43; 9.13 `review`/unmerged). AC0 decisions: (0a) per-map = weighted aggregate; (0b) `_normalize_hud` = lowercase + strip trailing `.0`. 9.13 cross-story collision resolved (Stephane): soften AC5 `name`→`id` to add `id`+`weight`/`weight_override` while retaining `name` read-alias + `name=` kwarg-alias so 9.13's uncommitted contract-net stays green; develop on current tree; no scope-creep into 9.13. epic-9 stays `in-progress`. Implementation in flight.  # Prior entry: CREATE-STORY 2026-05-17 (Stephane, /bmad-create-story on 9.14): flipped `backlog → ready-for-dev`. Story file `_bmad-output/implementation-artifacts/9-14-roi-detection-tester-refit-for-unified-schema.md` finalized — see the sprint-status header `last_updated: 2026-05-17 (Story 9.14 ...)` for the full create-story summary: 15 ACs / 7 Tasks; AC0 rebases Tool 9 input from the retired Tool-8 `discovered_zones.yaml` to the emitted unified `map_config.<hud_version>.json` (`--config` replaces `--zones`); 0a weighted-per-map (recommended) and 0b `_normalize_hud` (`v2.0`↔`v2`) are kickoff decisions; the 4-way game-state cascade + `TARGET_CLASSES` are DELETED (binary `in_match` collapse only in `_folder_to_in_match`; Tool 6 labeler untouched); cross-story frozen-symbol constraint with 9.13 (`zone_fires_on_frame`/`_argmax_with_threshold`/`_resize_to_ref` names+signatures pinned by 9.13's contract test). Deps SATISFIED (9.9c `9b9d4af` + 9.11 `aca0906` + 9.12 `546e467` on `main`, pytest baseline 132); fully unblocked, parallel-safe with 9.13. All 15 ACs `[ ]`; AC12/13/14 `[HELD]`. Two-PR + local `--no-ff` per [[feedback_two_pr_docs_execution]] + 9.9c/9.11/9.12 precedent. epic-9 stays `in-progress`. No code touched in this skill run.  # Prior entry: NEW 2026-05-15 (Stephane, /bmad-correct-course). Refit `apps/tooling/tools/roi_detection_tester.py` (Tool 9) for the unified schema. Three classifiers: (a) HUD-version (NEW) — ground truth = `v<hud_version>/` parent directory of each labeled frame; predicted = running `hud_version_detection` zones; (b) binary `in_match` / `not_in_match` (REPLACES existing 4-way `{lobby, in_match, score, transition}` cascade classifier) — ground truth = `true` if frame's folder is in MAP_LABELS, `false` otherwise (`lobby`/`score`/`transition` folders); predicted = `in_match_detection` zones argmax_above_threshold; (c) per-map ID (UNCHANGED). Tool 6 labeler stays UNCHANGED — still produces 4-folder structure (lobby/score/transition/<map>); the binary collapse happens in Tool 9's ground-truth derivation. `report.json` schema rewritten with three classifier sections (`hud_version_classifier`, `in_match_classifier`, `map_id_classifier`) — old 4-way `game_state_classifier` section removed (or renamed to `in_match_classifier` with binary confusion). `summary.md` rewritten in canonical order: HUD-version → in_match → map-ID. `frame_predictions.csv` opt-in columns: `frame_path, ground_truth_hud_version, predicted_hud_version, ground_truth_in_match, predicted_in_match, ground_truth_map_id, predicted_map_id` + per-classifier confidence. Pytest refit: existing 4-way game-state tests deleted/rewritten as binary; new HUD-version tests added; per-map tests preserved. Empty-zones backward-compat preserved (if `hud_version_detection` or `in_match_detection` is empty → classifier short-circuits to `unknown` for every frame + summary reports `accuracy 0.000 — zones unpopulated`, mirrors Tool 9 precedent). Dependencies: 9-9c (new schema). Sprint fit: fits-in-one-sprint (~1-2 days). Track C. See _bmad-output/sprint-change-proposal-2026-05-15.md.
  epic-9-retrospective: optional

  # ───────────────────────────────────────────────────────────────────
  # EPIC 10 — V1 Launch Readiness (10.4 external-blocked; 10.5 parallel demand-evidence track)
  # ───────────────────────────────────────────────────────────────────
  epic-10: backlog
  10-1-v1-launch-checklist-deliverable: backlog
  10-2-tos-monitoring-tracker-for-eva-after-h: backlog
  10-3-google-play-v1-review-readiness-checklist: backlog
  10-4-stripe-production-activation: backlog
  10-5-demand-evidence-capture-pre-v1: backlog
  epic-10-retrospective: optional
