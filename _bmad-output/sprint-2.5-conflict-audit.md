# Sprint 2.5 Per-Story Conflict Audit

**Generated:** 2026-05-09
**Author:** Stephane (executed by Amelia, BMM dev agent — `claude-opus-4-7[1m]`)
**Source story:** `_bmad-output/implementation-artifacts/0-1-conduct-per-story-conflict-audit-for-sprint-2-5-in-flight-mobile-work.md`
**PRD reference:** `_bmad-output/prd.md` (§2 No-free-tier; §5 Reader-App + on-device-only; §9 entitlement; §10 telemetry)
**Sprint plan reference:** `_bmad-output/sprint-plan.md` §2 Gate G0
**Live-status read date:** 2026-05-09 (each row's status was read from the legacy story file's `Status:` line, not from the stale `epics-and-stories.md` Implementation Notes)

## Audit Rules Applied

- **Rule A:** Free-tier or pre-no-free-tier flow → re-scope or drop. Anchors: PRD §2 "No-free-tier positioning" (line 164); PRD §5 Reader-App contract (lines 114, 202, 508–514).
- **Rule B:** Contradicts a locked PRD constraint → re-scope or drop. Constraints:
  - **B.1** Reader-App contract — no monetization surface in mobile build artifacts (PRD §5).
  - **B.2** On-device-only video / audio processing — no frames or voice cross any wire (PRD §5 on-device-only language; cross-referenced in `cross-MAP-CONFIG-DELIVERY-001` and the architecture's "On-device-only" structural commitment in `_bmad-output/architecture.md`).
  - **B.3** Six-state entitlement model: `paid` / `lapsed` / `offline-grace ≤30d` / `payment-failed` / `multi-device` / `signed-out` (PRD `cross-ENTITLEMENT-001`, line 980).
  - **B.4** 14-canonical-maps single-sourced from `tools/frame_labeler.py:19-34` `MAP_LABELS` (PRD `tooling-LABEL-002`, line 951).
  - **B.5** 3-value `view_mode` — Full / Minimap / Minimap+HUD; no 2-value `pov` literals (PRD `mobile-CINEMA-002` / `mobile-CINEMA-004`, lines 883, 885).
  - **B.6** Dual-T1 activation telemetry — payload schema `{elapsed_seconds, t0_at, t1_at, t1_path: 'coach' | 'active_player'}`; no frame data, no raw audio. PRD `cross-ACTIVATION-001` / `cross-ACTIVATION-002` (lines 985–986) define the events conceptually; the verbatim payload schema is defined in `_bmad-output/architecture.md` (lines 711–712, 1144) and `_bmad-output/epics-and-stories.md` (line 324).
- **Rule C:** Wholly compatible with the unified PRD → `complete-as-legacy`. The story ships under its existing AC; no rewrite, no AC modification.

Rules walk in order **A → B → C**. The **first rule that fires** determines the disposition.

## Audit Table

| story_id | legacy_AC_summary (incl. live status) | unified_PRD_conflict | conflict_specifics | disposition | target_epic | responsible_owner |
|---|---|---|---|---|---|---|
| 2.2 | **Status: `in-progress`** (sandbox-tractable work merged 2026-05-04: `@wokcito/ffmpeg-kit-react-native@6.1.2` resolves the JitPack deadlock via Maven Central; service-boundary audit + typecheck + jest pass; CR fixes applied for shell-injection / typed interop / sessionId path-traversal). Device-bound ACs 3, 4, 6 (smoke test on Poco X5, peak RAM < 2 GB per NFR5, boot regression check) deferred to dev handoff. AC contract: FFmpeg native module installed, `extractKeyframes`/`extractFrameAt`/`getVideoDuration`/`getProcessingDir`/`cleanupProcessingFiles` remain the sole entry point with stable shape; downstream `processingPipeline.ts` consumers untouched. | no | — | complete-as-legacy | — | Stephane |
| 2.5 | **Status: `ready-for-dev`**. Already rewritten 2026-05-05 to consume Story 7.5's `gameDetector` (KDA/HSV START/END events) and `mapIdentifier` (pHash) — supersedes the originally-planned `blackScreenDetector` (2.3) + `templateMatcher` (2.4), both `superseded`. AC: pair START/END into game segments, identify lobby/between-round time as excluded, persist segments into `map_segments` SQLite table with `map_index`/`start_time_ms`/`end_time_ms`/`map_name`, persist `null` for unknown maps (AC4) so Card View renders "Unknown map" while keeping the segment navigable, extract result-frame at `end_ts + score_offset_s` (Story 7.5 AC5). Pipeline orchestrated via `processingPipeline.ts`; CASCADE-protected deletes via existing `sessionRepository`. | no | — | complete-as-legacy | — | Stephane |
| 2.6 | **Status: `ready-for-dev`**. Background-processing screen with progress bar + rotating tips (5–8s cycle), Android foreground service via Expo config plugin (NFR4), MMKV-checkpoint resume across crash (FR10/NFR7), NFR1 < 2 min on Poco X5 for 1h20 video, auto-navigate to Card View on completion. Foreground-service mechanism aligns with the Sprint 3 Epic 1 Story 1.2 (`foreground-service-android-config-plugin`) but the legacy AC does not require that story's exact deliverable — it requires the screen + checkpoint + auto-nav, which are independent of the plugin packaging form. | no | — | complete-as-legacy | — | Stephane |
| 2.7 | **Status: `ready-for-dev`**. Local session list + management on `HomeScreen`: list with name + status badge + date; delete via swipe-or-icon with confirmation; `sessionRepository.deleteSession()` relies on existing SQLite `ON DELETE CASCADE` for `map_segments` / `clip_exports` / `audio_comments`; the source video file is NOT deleted (only SQLite records); cold-start UX shows "Resume last review" + "Import new session"; empty state shows "Import your first training session". Cascade behaviour foreshadows AR-12 (Sprint 3 Epic 6 Story 6.9 clip-deletion-cascade) but does not implement it; entirely local data — no server calls, no telemetry, no monetization surface. | no | — | complete-as-legacy | — | Stephane |
| 7.1 | **Status: `ready-for-dev`**. SQLite `clip_exports.view_mode` migration: `CHECK(view_mode IN ('pov', 'minimap'))` → `CHECK(view_mode IN ('full', 'minimap', 'minimap_hud'))`. Migration step `UPDATE clip_exports SET view_mode = 'full' WHERE view_mode = 'pov'` (rename, not loss) — wraps in transaction, recreates table via SQLite rename/copy/drop pattern (CHECK is not ALTER-able). Schema version bumped. Ships same PR as 7.2. This story **resolves** PRD constraint B.5 (it is the migration that retires the `pov` literal); it does not violate it. | no | — | complete-as-legacy | — | Stephane |
| 7.2 | **Status: `ready-for-dev`**. TypeScript `ClipExport.view_mode` union and exported `ViewMode` alias updated to `"full" \| "minimap" \| "minimap_hud"`. Codebase audit grep replaces all `'pov'` view_mode literals with `'full'`; `tsc --noEmit` clean. Pure type-safety change — no UI copy, no runtime behaviour. Ships same PR as 7.1. **Resolves** B.5 at the type layer. | no | — | complete-as-legacy | — | Stephane |
| 7.3 | **Status: `ready-for-dev`**. Cinema-Mode view-mode toggle UI: primary 44×44 icon button (top-right) toggles Full ↔ Minimap; secondary `HudToggle` chip is rendered only when in a Minimap variant and toggles HUD on/off; HUD-on preference restored when re-entering Minimap from Full (MMKV `prefs.minimapHud`); transition < 100ms, no animation, no debounce; Zustand playback store + MMKV persist. Aligns with PRD `mobile-CINEMA-002` (3-value control), `mobile-CINEMA-003` (unknown-map default-to-Full path is implied via Story 7.5/2.5 → mapIdentifier returning `null` → segment navigable as Full), and PERF-003 (≤100ms). Supersedes legacy 3.5. | no | — | complete-as-legacy | — | Stephane |
| 7.4 | **Status: `done`** (epics file lists `ready-for-dev` — stale; legacy story file is authoritative). Already merged. Remote DetectionConfig with stale-while-revalidate MMKV cache, offline-first-launch gate, singleflight on all network paths, cache-read re-validation, malformed-remote disambiguated from genuine offline (post-CR fixes). Provides typed `DetectionConfig` accessor consumed by Story 7.5 detectors. No further work for this audit's scope. | no | — | complete-as-legacy (already merged; no further work) | — | Stephane |
| 7.5 | **Status: `in-progress`** (epics file lists `ready-for-dev` — stale; legacy story file is authoritative). KDA/HSV `gameDetector` (white-pixel ratio on `kda` ROI ≥ `min_ratio`, `notkda` ROI grayscale mean < `hud_brightness_max`, START/END confirm-frame counters) + pHash `mapIdentifier` (Hamming-distance against `DetectionConfig.maps` below `collision_threshold`) + 3-state long-GOP `blackScreenDetector` fallback (Pass-1 `team_bar` saturation, Pass-2 in-window 3-state FSM) wired through `processingPipeline.ts`. Sprint 2 `templateMatcher.ts` and `assets/images/map-templates/` are deleted per AC9 (cleanup of pre-pHash template-matching path). DetectionConfig schema gains `collision_threshold` + `team_bar_min_sat`. **Open follow-up:** Review Follow-up [HIGH] — `loadFrameFromPath` needs `react-native-fast-opencv` native-module wiring before end-to-end on real video; covered today only by injected synthetic FrameLoaders in unit tests (13 suites / 106 tests pass). This native-binding wiring is Epic 1 Story 1.1 (AR-spike) territory, not a 7.5 disposition concern. | no | — | complete-as-legacy | — | Stephane |
| 7.6 | **Status: `ready-for-dev`**. Export-pipeline recipes via `exportRecipes.ts` (pure FFmpeg argv builder) + orchestration via `exportPipeline.ts`: Full → trim + transcode (`libx264 -preset fast -crf 23`, `-c:a copy`); Minimap → crop with `minimap` ROI scaled to source resolution + scale to export resolution; Minimap+HUD → `-filter_complex` overlaying KDA + Score (points) ROIs onto the minimap base. Status lifecycle `defining → locked → exporting → ready` persisted via `clipExportRepository`; partial output deleted on FFmpeg non-zero exit; output is standard MP4 (FR25). On-device FFmpeg encode only (no cloud encode). Audio is `-c:a copy` (voice-annotation merge is Sprint 4 — explicitly out of scope here per Dev Notes "Do NOT merge clip voice-over audio here"). Supersedes view-mode portion of legacy 5.1 + 5.2. | no | — | complete-as-legacy | — | Stephane |

## Summary counts

- Total rows: **10**
- `complete-as-legacy`: **10**
- `re-scope-into-Sprint-3-with-new-AC`: **0**
- `drop`: **0**

**Reviewer-skepticism counter-evidence:** All 10 stories pass Rule C. This outcome matches the Dev Notes "Realistic disposition expectations" prior — the 2026-05-05 sprint-change-proposal (committed at `7b5ce30 Propagate Proposals 4+6 (KDA/HSV + pHash) through planning artifacts`, plus the proposal artifact at `_bmad-output/legacy/mobile/planning-artifacts/sprint-change-proposal-2026-05-05.md`) pre-aligned the Sprint 2.5 in-flight stories to the methodology pivot (3-value `view_mode`, KDA/HSV `gameDetector`, pHash `mapIdentifier`, DetectionConfig shim) **before** the unified PRD existed. The audit rules walk per-story confirms this alignment rather than discovering it. Per-row `legacy_AC_summary` columns above carry substantive AC summaries (not "no conflicts found") so a manual review pass can verify the auditor read each file. Stephane's manual sign-off against PRD §3 / §5 / §9 / §10 (epic 0.1 acceptance) is the verification step.

## Sprint 3 merge-block status

Until Story 0.2 executes these dispositions and is marked `done`, no Sprint 3 story may merge to `main` per Sprint Plan §2 Gate G0.

## G0 sign-off

Story 0.2 executed all 10 dispositions on 2026-05-09 (date of merge — substitute actual merge date if different). Sprint Plan §2 Gate G0 is now CLEARED. Sprint 3 stories may merge to `main` per the audit's binding dispositions.
