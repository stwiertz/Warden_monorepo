# Story 7.3: View Mode Toggle UI (Full / Minimap / Minimap+HUD)

Status: ready-for-dev

<!-- sprint-3-disposition: complete-as-legacy -->
> **Sprint 3 disposition (audit `2026-05-09`):** `complete-as-legacy`. AC unchanged. Live status at audit time: `ready-for-dev`. Source audit row: [`_bmad-output/sprint-2.5-conflict-audit.md`](../../../sprint-2.5-conflict-audit.md) row `7.3` (Story 0.1, [PR #1](https://github.com/stwiertz/Warden_monorepo/pull/1), merge commit `3afda01`). Disposition executed: Story 0.2 (`_bmad-output/implementation-artifacts/0-2-execute-sprint-2-5-per-story-dispositions.md`).

## Story

As a coach reviewing a session in Cinema Mode,
I want a toggle button to switch between full-screen view and minimap view, with a secondary control to show or hide the HUD over the minimap,
so that I can focus on the perspective most useful for analysis without leaving playback.

## Acceptance Criteria

1. **Given** the Cinema Mode player is visible, **when** the controls overlay is shown, **then** a primary view-mode button is rendered (icon-only, 44x44 touch target, top-right region).
2. **Given** the primary button shows `Full`, **when** the coach taps it, **then** the view switches to `minimap` (HUD off by default or remembered preference — see AC 6).
3. **Given** the primary button shows `Minimap` (either `minimap` or `minimap_hud`), **when** the coach taps it, **then** the view switches to `full`.
4. **Given** the current view is `minimap` or `minimap_hud`, **when** the controls overlay is shown, **then** a secondary `HUD on/off` chip is rendered adjacent to the primary button.
5. **Given** the current view is `full`, **when** the controls overlay is shown, **then** the `HUD on/off` chip is NOT rendered.
6. **Given** the coach previously chose `HUD on`, **when** they later switch from `full` → `minimap`, **then** the view restores to `minimap_hud` (HUD preference persists via MMKV `prefs.minimapHud`).
7. **Given** any view-mode transition, **when** the tap is registered, **then** the visual change is applied in < 100ms (no animation, no debounce).
8. **Given** the view mode changes, **then** the Zustand playback store is updated and the change is persisted (via MMKV) so it survives app restart.

## Tasks / Subtasks

- [ ] Task 1: Extend the Zustand playback store (AC: 6, 8)
  - [ ] Add `viewMode: ViewMode` and `setViewMode(mode: ViewMode)` to the playback store
  - [ ] Add `minimapHud: boolean` with `setMinimapHud(on: boolean)` (derived state; persists separately so preference survives when primary mode flips to `full`)
  - [ ] Configure MMKV persist for both keys (`playback.viewMode`, `prefs.minimapHud`)
- [ ] Task 2: Build `ViewModeToggle.tsx` (AC: 1, 2, 3, 7)
  - [ ] Create `src/features/video-playback/ViewModeToggle.tsx`
  - [ ] Render an icon button using the shared `Button` component
  - [ ] Icon: full-screen glyph when active mode is `full`; map-pin glyph when active mode is `minimap` or `minimap_hud`
  - [ ] On tap: if current is `full`, set to `minimap` if `minimapHud` is false, else `minimap_hud`. If current is a minimap variant, set to `full`.
- [ ] Task 3: Build `HudToggle.tsx` (AC: 4, 5, 6)
  - [ ] Create `src/features/video-playback/HudToggle.tsx`
  - [ ] Render only when `viewMode !== 'full'`
  - [ ] Chip/switch styling per design tokens (small, accent when on)
  - [ ] On toggle: update `minimapHud` AND update `viewMode` to `minimap` or `minimap_hud` accordingly
- [ ] Task 4: Integrate into Cinema Mode controls (AC: 1, 4, 5)
  - [ ] Add both components to the Cinema Mode controls overlay container
  - [ ] Layout: `ViewModeToggle` on top-right; `HudToggle` to its left when visible
- [ ] Task 5: Test view-mode behaviour (AC: 2, 3, 6, 7)
  - [ ] Unit test: tapping primary in `full` produces `minimap` (hud off) or `minimap_hud` (hud on preference)
  - [ ] Unit test: tapping primary in either minimap variant produces `full`
  - [ ] Unit test: HUD preference persists across a simulated app restart (rehydrated MMKV)

## Dev Notes

- **Not yet built**: The Cinema Mode player itself is planned for Sprint 3 (currently pending). This story slots INTO that work rather than preceding it — coordinate sequencing with Story 3.x (playback). If Sprint 3 is in flight, merge via feature flag; otherwise build the toggle components first and wire into the playback screen when it lands.
- **Icon assets**: Use existing iconography from `src/shared/components/` or the project icon set — do not import new icon libraries.
- **Animation**: Explicitly none. The requirement is an instant visual swap; transitions would obscure the pixel shift between crop regions.
- **Persistence separation**: `playback.viewMode` is session-scoped (what the coach is looking at right now); `prefs.minimapHud` is user-wide (their preference for HUD on/off inside minimap view).

### Project Structure Notes

- New files:
  - `src/features/video-playback/ViewModeToggle.tsx`
  - `src/features/video-playback/HudToggle.tsx`
- Store edit: existing playback store (create if not yet present; Sprint 3 work)
- Uses shared `ViewMode` type from `src/shared/types/index.ts` (Story 7.2)

### References

- [Source: src/shared/types/index.ts] — `ViewMode` alias after Story 7.2
- [Source: tailwind.config.ts] — design tokens (44x44 touch target, #FF6B00 accent)
- [Source: docs/planning-artifacts/ux-design-specification.md] — Cinema Mode control overlay pattern
- [Proposal 6 — approved 2026-04-20] — two-level toggle (Full↔Minimap + HUD on/off chip)

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
