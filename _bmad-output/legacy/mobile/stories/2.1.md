# Story 2.1: Import MP4 Video from Device Storage

Status: ready-for-dev

## Story

As a coach,
I want to import an MP4 video file from my device storage,
so that I can begin reviewing my training session.

## Acceptance Criteria

1. **Given** the coach is on the home screen, **when** the coach taps the import button, **then** the device file picker opens filtered to video files.
2. **Given** a file is selected, **when** the file is validated, **then** only MP4 (H.264/AAC) files are accepted (FR4).
3. **Given** an incompatible format is selected, **when** validation fails, **then** a critical error dialog is shown with a clear message (FR4).
4. **Given** a valid MP4 is selected, **when** import begins, **then** a new session record is created in SQLite with status `importing`.
5. **Given** the session is created, **then** the video file is referenced in-place (not copied) per architecture spec.
6. **Given** the session is created, **then** the coach is navigated to the processing screen.

## Tasks / Subtasks

- [ ] Task 1: Create video import service (AC: 1, 2, 3)
  - [ ] Create `src/features/video-import/videoImportService.ts`
  - [ ] Implement file picker using `expo-document-picker` filtered to `video/mp4`
  - [ ] Implement MP4 format validation (check MIME type and file extension)
  - [ ] Return validation result with error message for incompatible formats
- [ ] Task 2: Create video import hook (AC: 1, 4, 5, 6)
  - [ ] Create `src/features/video-import/useVideoImport.ts`
  - [ ] Wire file picker trigger → validation → session creation → navigation
  - [ ] Call `sessionRepository.createSession()` with the video file path (in-place reference)
  - [ ] Navigate to processing screen on success
- [ ] Task 3: Create import UI on HomeScreen (AC: 1, 3, 6)
  - [ ] Add import button to `src/app/screens/HomeScreen.tsx`
  - [ ] Show error dialog (not toast) for incompatible formats using existing Dialog component
  - [ ] Navigate to processing screen placeholder after successful import
- [ ] Task 4: Create types (AC: all)
  - [ ] Create `src/features/video-import/types.ts` with `ImportResult`, `ValidationError` types

## Dev Notes

- **File picker**: Use `expo-document-picker` with `type: ['video/mp4']`. Do NOT use `expo-image-picker` — it's for photos/camera, not file selection from storage.
- **In-place reference**: Store the file URI returned by the picker directly in `sessions.video_file_path`. Do NOT copy the file to app storage.
- **Session creation**: Reuse existing `sessionRepository.createSession(videoFilePath, name)` from Sprint 1 — it already creates a record with status `importing`.
- **Error handling**: Use a Dialog (modal) for format errors per UX spec ("critical error dialog"), not a toast. Toast is for non-blocking feedback.
- **Processing screen**: Story 2.6 will build the full processing screen. For now, navigate to a placeholder or the session detail route.

### Project Structure Notes

- New files go in `src/features/video-import/` (currently has only `.gitkeep`)
- Reuse `src/features/session/sessionRepository.ts` for `createSession()` — do NOT duplicate session CRUD
- Reuse `src/shared/types/index.ts` `Session` type — already defines the full schema
- HomeScreen exists at `src/app/screens/HomeScreen.tsx`

### References

- [Source: docs/planning-artifacts/architecture.md#Data Architecture] — video file referenced in-place
- [Source: docs/planning-artifacts/architecture.md#SQLite Schema] — sessions table with status `importing`
- [Source: docs/planning-artifacts/epics.md#Story 2.1] — full acceptance criteria
- [Source: docs/planning-artifacts/architecture.md#Error Handling Patterns] — validation format at entry, clear message if incompatible
- [Source: src/features/session/sessionRepository.ts] — existing `createSession`, `deleteSession`, `getAllSessions`

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
