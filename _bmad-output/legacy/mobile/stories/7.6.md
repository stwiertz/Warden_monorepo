# Story 7.6: Export Pipeline for Full / Minimap / Minimap+HUD

Status: ready-for-dev

## Story

As a coach,
I want to export a clip in the exact view mode I chose (full, minimap, or minimap with HUD overlay),
so that the shared video matches the perspective I was reviewing and highlights the information I want my audience to focus on.

## Acceptance Criteria

1. **Given** a `ClipExport` row with `view_mode = 'full'`, **when** export runs, **then** FFmpeg trims the source between `start_time_ms` and `end_time_ms` and transcodes using the row's `export_quality` without any crop.
2. **Given** a `ClipExport` row with `view_mode = 'minimap'`, **when** export runs, **then** FFmpeg crops the source using the `minimap` ROI from `DetectionConfig` (scaled to the source resolution) and scales the crop to the export resolution.
3. **Given** a `ClipExport` row with `view_mode = 'minimap_hud'`, **when** export runs, **then** FFmpeg composes the minimap crop (as base) with the `kda` ROI and the `points` (score) ROI overlaid at their scaled positions within the final frame.
4. **Given** any view mode, **when** the source has embedded audio, **then** the audio track is copied (no re-encode) unless trimming requires a re-encode for sync.
5. **Given** export starts, **when** the FFmpeg process emits progress, **then** the pipeline updates `ClipExport.status` through the lifecycle `defining` → `locked` → `exporting` → `ready` and updates `file_path` on completion.
6. **Given** export fails, **when** the FFmpeg process exits non-zero, **then** `ClipExport.status` is NOT advanced past `exporting`, the error is logged, and the partial output file is removed.
7. **Given** `export_quality = 'mobile'`, **when** export runs, **then** the output is 720p @ ~CRF 23, `libx264 -preset fast`. Given `export_quality = 'hd'`, **then** the output is the source resolution with the same codec settings.
8. **Given** the export finishes, **then** the output file is playable in any standard MP4 player without the Warden app installed (FR25).

## Tasks / Subtasks

- [ ] Task 1: Build the export recipe builder (AC: 1, 2, 3, 7)
  - [ ] Create `src/features/clip-export/exportRecipes.ts`
  - [ ] Export a pure function `buildExportCommand(clip: ClipExport, config: DetectionConfig, sourcePath: string, outputPath: string): string[]` returning FFmpeg argv
  - [ ] Full-mode recipe: `-ss`, `-to`, `-c:v libx264 -preset fast -crf 23`, `-c:a copy`
  - [ ] Minimap-mode recipe: add `-vf "crop=W:H:X:Y,scale=<target>"` with ROI scaled to source resolution
  - [ ] Minimap+HUD recipe: `-filter_complex` with `split` + `overlay` for `kda` and `points` ROIs; refer to DetectionConfig for coordinates
- [ ] Task 2: Build the export service (AC: 4, 5, 6)
  - [ ] Create `src/features/clip-export/exportPipeline.ts`
  - [ ] Function `exportClip(clipId: string): Promise<ClipExport>`
  - [ ] Load the clip via `clipExportRepository.get(clipId)`
  - [ ] Load the detection config via the service (Story 7.4)
  - [ ] Transition status: `locked` → `exporting` (persist immediately)
  - [ ] Build the command (Task 1) and execute via `ffmpeg-kit-react-native`
  - [ ] Parse FFmpeg progress; for long exports, update a transient progress surface (does NOT need to be persisted to SQLite — just expose via Zustand/event emitter)
  - [ ] On success: set `file_path`, transition to `ready`
  - [ ] On failure: keep status at `exporting`, log, delete partial file
- [ ] Task 3: HUD overlay offset math (AC: 3)
  - [ ] The minimap crop becomes the full output frame; `kda` and `points` overlays must be placed at output-frame coordinates, NOT source coordinates
  - [ ] Helper: `computeOverlayPlacement(sourceRoi: ROI, minimapRoi: ROI, outputSize: { w, h }): { x, y, w, h }` — decides where in the final frame each overlay sits (either scaled-in-place if they'd fall inside the minimap crop, or composited into a margin)
  - [ ] Document the chosen placement in code comments — this is a genuinely non-obvious decision
- [ ] Task 4: Repository plumbing (AC: 5)
  - [ ] Add or reuse `clipExportRepository.update(id, changes)` to persist status transitions and `file_path`
- [ ] Task 5: Tests (AC: 1, 2, 3, 5, 6, 7)
  - [ ] Unit test: `buildExportCommand` for each view mode produces the expected argv (golden-file style)
  - [ ] Unit test: status lifecycle on success and failure
  - [ ] Integration test (if FFmpeg wrapper supports simulation): exercise a 1-second fixture through each recipe and assert output exists and is non-empty
- [ ] Task 6: Wire into export trigger UI (AC: 5)
  - [ ] The Sprint 5 export button (when built) should invoke `exportClip()` — this story does NOT build the UI, but provide a thin hook `useClipExport(clipId)` returning status + progress for the UI to consume

## Dev Notes

- **HUD overlay choice**: Approved overlays for `minimap_hud` are **KDA** and **Score (points)** only (Proposal 9). Do NOT add map-name banner or team-bar overlays — those were explicitly excluded.
- **Overlay placement**: The minimap crop is ~234x38 at 1080p — very narrow. KDA and Score ROIs will NOT fit inside that crop scaled to 1280x720. Expect the implementation to position them in a small overlay band at the bottom of the output frame, scaled proportionally. Exact layout is Task 3's responsibility.
- **Dependency on Stories 7.1, 7.2, 7.4**: 7.1/7.2 give us the `minimap_hud` value; 7.4 gives us the ROI coordinates.
- **Why `-c:a copy`**: Re-encoding audio breaks lip-sync with voice commentary (Sprint 4 work). Copy is the safe default; fall back to re-encode only if the container requires it.
- **Partial-file cleanup**: FFmpeg may leave a zero-length or corrupt file on failure. Always attempt to delete it in the catch path, and tolerate the delete itself failing silently.
- **Do NOT merge clip voice-over audio here**: Voice commentary merging is a later concern (see Sprint 4). This story is the video side only.

### Project Structure Notes

- New files:
  - `src/features/clip-export/exportRecipes.ts` — pure argv builder
  - `src/features/clip-export/exportPipeline.ts` — orchestration
  - `src/features/clip-export/useClipExport.ts` — hook (optional, for UI)
- Reuses:
  - `src/shared/services/ffmpeg.ts` — existing FFmpeg wrapper
  - `src/features/clip-export/clipExportRepository.ts` — create if not present (trivial CRUD)

### References

- [Source: src/shared/services/ffmpeg.ts] — existing FFmpeg wrapper
- [Source: src/shared/types/index.ts] — `ClipExport` type (after Story 7.2)
- [Story 7.4] — `DetectionConfig` ROIs for crop/overlay coordinates
- [Proposal 9 — approved 2026-04-20] — three recipes; `minimap_hud` overlays = KDA + Score

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
