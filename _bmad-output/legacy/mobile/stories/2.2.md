# Story 2.2: Integrate FFmpeg and Extract Keyframes

Status: in-progress  <!-- agent finished sandbox-tractable work; device-side ACs 3/4/6 still pending — see Dev Agent Record "Device handoff" -->


## Story

As a developer,
I want a working FFmpeg integration on the Android dev client with reliable keyframe extraction,
so that downstream stories (2.3 black screen detection, 2.4 template matching, 2.5 segmentation, 2.6 background processing) can analyze video frames without decoding the full video and within the 2 GB RAM budget (NFR5).

## Context: What's already in tree (read before starting)

> Sprint 2 was scaffolded all-at-once in commit `7eef587 Implement Sprint 2: Video Import & Auto-Slice Processing pipeline`. The FFmpeg surface area for this story exists in code, but it is **currently dead code** — the dependency is not installed and the config plugin is not registered. Commit `0c75f5d Fix boot crash and align deps for RN 0.81 dev build` (the most recent commit on `main`) explicitly removed `with-ffmpeg` from `app.json` with this rationale:
>
> > _"Drop with-ffmpeg from app.json plugins: the jdarshan5/ffmpeg-kit-react-native 6.0-2 artifact isn't published on JitPack, so the Sprint 2 video pipeline can't build until this is resolved."_
>
> So this story is **not** "create from scratch" — it is **"make the existing FFmpeg surface actually build and run"**. The service contract (`extractKeyframes`, `extractFrameAt`, `getVideoDuration`, `getProcessingDir`, `cleanupProcessingFiles`) must remain stable because `src/features/video-processing/processingPipeline.ts` already imports it.

**Already in tree:**

- `plugins/with-ffmpeg.js` — Expo config plugin using `withAppBuildGradle`, currently injects `implementation 'com.github.jdarshan5:ffmpeg-kit-react-native:6.0-2'` (this Maven coordinate does not resolve — see AC1).
- `src/shared/services/ffmpeg.ts` — service wrapper, lazy-`require`s `ffmpeg-kit-react-native`, throws "FFmpeg native module not available. Run expo prebuild and build the dev client." if missing. Implements `extractKeyframes`, `extractFrameAt`, `getVideoDuration`, `cleanupProcessingFiles`, `getProcessingDir`.
- `src/features/video-processing/processingPipeline.ts` — already calls all four FFmpeg functions; do **not** refactor it as part of this story.
- `src/features/video-processing/types.ts` — defines `KeyframeInfo { path, timestampMs }` (return shape from `extractKeyframes`); do **not** change this shape.
- `app.json` — `plugins: []` (empty array). The config plugin is currently NOT registered.
- `package.json` — `ffmpeg-kit-react-native` is **NOT** listed in dependencies.

**Not yet in tree:**

- A working FFmpeg native dependency that resolves on Android Gradle.
- Any registration of `with-ffmpeg.js` in `app.json` plugins.
- A successful `expo prebuild` + `expo run:android` cycle that boots without crashing.
- Any test or evidence that keyframe extraction actually runs on a real device.

## Acceptance Criteria

1. **Given** `package.json`, **when** the developer runs `npm install`, **then** an FFmpeg React Native module is installed whose Android Maven coordinate resolves on a real Gradle build (no `Could not find ...` errors during `expo run:android`). _The architecture's preferred choice is the `jdarshan5/ffmpeg-kit-react-native` community fork, but the `6.0-2` JitPack artifact does not exist — the developer MUST verify the chosen artifact resolves before declaring this AC met. See Dev Notes "FFmpeg dependency resolution" for the decision tree._
2. **Given** the FFmpeg native module is installed via AC1, **when** `expo prebuild --clean` runs, **then** Gradle resolves the FFmpeg AAR for `com.anonymous.Warden` — either (a) via React Native autolinking with no Expo config plugin, OR (b) via `plugins/with-ffmpeg.js` patching `android/app/build.gradle` and registered in `app.json` `plugins`. _Updated 2026-05-04: chosen library (`@wokcito/ffmpeg-kit-react-native`) auto-links, so option (a) is the satisfied form — `plugins/with-ffmpeg.js` was deleted and `app.json` `plugins` remains `[]`. See Change Log + Completion Notes._
3. **Given** an MP4 imported via Story 2.1 (`videoImportService.ts`), **when** `extractKeyframes(videoPath, sessionId)` is called, **then** the function returns a non-empty `KeyframeInfo[]` whose `path` files exist on disk and whose `timestampMs` values are monotonically non-decreasing. _Verified by manual smoke test on the dev client; no automated integration test required for this story._
4. **Given** keyframe extraction runs against a ≥30 min H.264/AAC test video on the reference device class (Snapdragon 695 / 6 GB RAM, Poco X5), **then** peak process RAM stays below 2 GB (NFR5). _Verified via Android Studio Profiler or `adb shell dumpsys meminfo` snapshots; record the peak in the Completion Notes._
5. **Given** any consumer of FFmpeg, **then** `src/shared/services/ffmpeg.ts` remains the **sole** entry point — no `require('ffmpeg-kit-react-native')` or equivalent appears anywhere else under `src/`. The exported function signatures (`extractKeyframes`, `extractFrameAt`, `getVideoDuration`, `getProcessingDir`, `cleanupProcessingFiles`) and the `KeyframeInfo` return shape do not change.
6. **Given** the dev client boots after this story lands, **then** the boot-time TypeError documented in commit `0c75f5d` does not return, AND `processingPipeline.runProcessingPipeline(sessionId)` no longer throws "FFmpeg native module not available" when called.

## Tasks / Subtasks

- [~] **Task 1: Resolve the FFmpeg dependency (AC: 1)** — investigation gate, do this first
  _(JS-side install complete; **Gradle-side resolution of the Maven Central AAR is verified in Task 3**, which is device-bound. AC1 is therefore PARTIAL until Task 3 runs on a real Android build.)_
  - [x] Try `jdarshan5/ffmpeg-kit-react-native` from npm and inspect its `android/build.gradle` to find the actual Maven coordinate it ships (the README + npm tarball, not architecture.md, are sources of truth)
  - [x] If the npm tarball does not work, evaluate alternatives in Dev Notes "FFmpeg dependency resolution" (other community forks, vendored AAR, Expo Modules wrapper)
  - [x] Pick exactly one strategy and document the choice + the working Maven coordinate in the Completion Notes
  - [x] Add the dependency to `package.json` and run `npm install` cleanly
- [x] **Task 2: Update the config plugin to match the chosen artifact (AC: 2)**
  - [x] Update `plugins/with-ffmpeg.js` to inject the working coordinate (replace `com.github.jdarshan5:ffmpeg-kit-react-native:6.0-2` if needed)
  - [x] If the chosen library auto-links via React Native autolinking and does not need a manual `implementation` line, simplify the plugin to a no-op (or delete it entirely and update `app.json` accordingly)
  - [x] Register the plugin in `app.json` `plugins` array (currently empty)
  - [ ] Run `npx expo prebuild --clean` and verify it completes without errors — **DEFERRED to device handoff (requires Android SDK)**
- [ ] **Task 3: Validate the Android dev client build end-to-end (AC: 1, 6)** — **BLOCKED: requires Android device/emulator + SDK, not executable in agent sandbox**
  - [ ] Run `npm run android` (which calls `expo run:android`) on a connected device or emulator
  - [ ] Verify the build completes (Gradle resolves the FFmpeg artifact)
  - [ ] Verify the app boots — no `TypeError: Cannot read property 'prototype' of undefined` regression from the MMKV/Nitro issue
  - [ ] Verify the Sprint 1 login + Sprint 2.1 import flows still work
- [ ] **Task 4: Smoke-test keyframe extraction (AC: 3, 4)** — **BLOCKED: requires physical device + Android Studio Profiler / adb**
  - [ ] Place a ≥30 min H.264/AAC `.mp4` test fixture on the device under `Download/`
  - [ ] Add a temporary debug button on `HomeScreen` (or use the existing import flow) to call `extractKeyframes(uri, 'smoke-test-session')` and `console.log` the count + first 5 entries — **delete the button before commit**
  - [ ] Confirm the keyframe count is plausible (`videoDurationSec / GOP` order of magnitude — typically 1 keyframe every 2-10 s) and that timestamps are monotonically non-decreasing
  - [ ] Inspect the output directory `${cacheDir}/processing/smoke-test-session/keyframes/` to confirm `.jpg` files exist
  - [ ] Capture a peak-RAM snapshot during extraction (Android Studio Profiler → Memory, or `adb shell dumpsys meminfo com.anonymous.Warden`); record peak in Completion Notes
- [x] **Task 5: Service-boundary audit (AC: 5)**
  - [x] `grep -rn "ffmpeg-kit-react-native" src/` and confirm only `src/shared/services/ffmpeg.ts` matches
  - [x] Confirm public exports of `src/shared/services/ffmpeg.ts` are unchanged (function names, parameter types, return types) — the existing `processingPipeline.ts` consumers must not break
  - [x] Run `npm run typecheck` — must pass with zero errors *(note: 3 pre-existing baseline errors in firebaseConfig.ts/storage.ts are not introduced by this story; story-touched files type-check clean)*
- [ ] **Task 6: Cleanup**
  - [ ] Remove the temporary debug button added in Task 4 — N/A until Task 4 runs
  - [x] Verify `npm test` still passes (existing tests are auth-only; this story does not add new tests — see Dev Notes "Testing approach")
  - [ ] Update Story 2.2 status to `done` only after all ACs verified on a real device

## Dev Notes

### FFmpeg dependency resolution (the hard part)

The architecture document picks `jdarshan5/ffmpeg-kit-react-native` as the MVP fork ([Source: docs/planning-artifacts/architecture.md#FFmpeg Strategy]). The previous Sprint 2 implementation (commit `7eef587`) hardcoded the JitPack coordinate `com.github.jdarshan5:ffmpeg-kit-react-native:6.0-2` in `plugins/with-ffmpeg.js`, but that coordinate does **not resolve** on JitPack — confirmed in commit `0c75f5d` and is the reason the FFmpeg plugin is currently disabled.

**Decision tree for the developer:**

1. **First preference:** install the npm package `jdarshan5/ffmpeg-kit-react-native` (or whatever name it ships under on npm — verify; the GitHub org is `arthenica`'s old fork ecosystem). Inspect its `android/build.gradle` inside `node_modules/` to find the actual `implementation` line it expects. RN autolinking may make the config plugin unnecessary.
2. **Second preference:** if the npm package's autolinking is enough, **delete** `plugins/with-ffmpeg.js` and leave `app.json` `plugins` empty. A no-op plugin is worse than no plugin.
3. **Third preference:** if a manual Gradle line is required, update `with-ffmpeg.js` to inject the actual coordinate the library expects (e.g., the published Maven Central coordinate, not a guessed JitPack one).
4. **If all forks of `ffmpeg-kit-react-native` are dead/unbuildable:** escalate before guessing. The architecture document lists three fallbacks ([Source: docs/planning-artifacts/architecture.md#Risk Alert: ffmpeg-kit-react-native Deprecated]):
   - Option B: Vendor the FFmpegKit AAR locally + custom config plugin
   - Option C: Custom native module via Expo Modules API wrapping FFmpeg
   Both are ≥1 sprint of extra work — flag in Completion Notes and request guidance rather than silently picking one.

**Do not** silently skip the FFmpeg integration or stub it out — Story 2.3 onward depends on real keyframe data.

### Why this story matters more than it looks

The Sprint 2 implementation in commit `7eef587` claimed all of 2.1-2.7 done. In practice:

- 2.1 (video import) genuinely works — no native FFmpeg dependency.
- 2.2 (this story) is dead code — nothing calls into native FFmpeg.
- 2.3 onward are stubs (per the commit message: _"Black screen detection (2.3) and template matching (2.4) are stubbed with proper interfaces — real detection algorithms will be integrated from an external script in Sprint 3"_) but they import from `ffmpeg.ts` and would crash at runtime today.

**So:** until this story closes for real (Android dev client builds AND `extractKeyframes` returns real frames), Sprint 2 is effectively blocked. Treat AC4 (RAM under 2 GB on real device) and AC6 (boot doesn't regress) as gating, not nice-to-have.

### Service contract — do NOT change

Other code already imports from `src/shared/services/ffmpeg.ts`. The contract this story must preserve verbatim:

```ts
// src/shared/services/ffmpeg.ts
export interface ExtractKeyframesOptions {
  width?: number;       // default 320
  quality?: number;     // 1-31, lower=better, default 5
}
export function getProcessingDir(sessionId: string): string;
export function extractKeyframes(
  videoPath: string,
  sessionId: string,
  options?: ExtractKeyframesOptions
): Promise<KeyframeInfo[]>;
export function extractFrameAt(
  videoPath: string,
  timestampMs: number,
  outputPath: string
): Promise<string>;
export function getVideoDuration(videoPath: string): Promise<number>;
export function cleanupProcessingFiles(sessionId: string): void;
```

Where `KeyframeInfo` comes from `src/features/video-processing/types.ts`:

```ts
export interface KeyframeInfo { path: string; timestampMs: number; }
```

If a chosen FFmpeg library forces an API change (different log parsing, different return type), preserve the public surface above by adapting internally — `processingPipeline.ts` and the future detectors should not need to change.

### FFmpeg command pattern (reference)

The current implementation uses:

```
ffmpeg -i {input} -skip_frame nokey -vsync vfr \
       -vf "scale=320:-1,showinfo" -qscale:v 5 \
       {outputDir}/frame_%04d.jpg
```

This is correct and matches the architecture spec. Do not change unless the chosen library requires syntax differences.

Timestamps are extracted from FFmpeg's stderr `pts_time:N.NN` log lines emitted by the `showinfo` filter. The current implementation already has this parser and a directory-listing fallback if log parsing fails — keep both.

### Filesystem API

`expo-file-system@^19` (currently installed) uses the new `Paths`/`Directory` API, NOT the legacy `FileSystem.cacheDirectory` string. The current `ffmpeg.ts` uses `Paths.cache.uri` and `new Directory(path)` — this is correct for v19. Do not regress to the legacy API.

Output directory layout (already implemented, do not change):

```
{Paths.cache.uri}/processing/{sessionId}/keyframes/frame_0001.jpg
                                       /frame_0002.jpg
                                       ...
{Paths.cache.uri}/processing/{sessionId}/results/map_0.jpg     (extracted in Story 2.5)
                                                /map_1.jpg
```

The cache directory is OS-clearable, which is the correct location per architecture: keyframes are recomputable, not user data.

### Testing approach for this story

This story is fundamentally a **native integration** story — its correctness lives in the Gradle build and on a real device. Unit tests against `ffmpeg.ts` would be testing a mocked native module, which has near-zero value. **Skip unit tests for this story.**

What replaces tests:

- Manual smoke test on a real Android device or emulator (Task 4)
- `npm run typecheck` to confirm the public surface didn't drift
- Existing Jest suite (`npm test`) must still pass (currently auth-only)

When Story 2.3 lands, the black-screen detector becomes the natural unit-testable seam — feed it synthetic `KeyframeInfo[]` arrays and assert range output. That's where coverage belongs, not here.

### NFR5 (RAM < 2 GB) — what to actually measure

- The `-vf scale=320:-1` flag is the primary RAM control: low-res frames keep individual decoded buffers small.
- `-skip_frame nokey` ensures only I-frames are decoded, skipping P/B frames entirely.
- FFmpeg natively pipelines frame-by-frame, so RAM should sit well below 2 GB even for 1h20 videos.
- If profiling shows RAM > 2 GB, the most likely culprits are: (a) JS-side accumulating all keyframes in a single array (acceptable — `KeyframeInfo[]` is small), (b) the chosen FFmpeg library leaking session/log buffers across calls (less acceptable — flag and investigate).

### Project Structure Notes

- `plugins/with-ffmpeg.js` — exists, may be modified or deleted depending on autolinking
- `src/shared/services/ffmpeg.ts` — exists, modify internals only, preserve public surface
- `src/features/video-processing/types.ts` — `KeyframeInfo` shape is locked
- No new files expected unless escalating to Plan B/C (vendored AAR or Expo Modules wrapper)

## References

- [Source: docs/planning-artifacts/architecture.md#FFmpeg Strategy] — fork choice rationale, Plan B options
- [Source: docs/planning-artifacts/architecture.md#Risk Alert: ffmpeg-kit-react-native Deprecated] — three viable strategies (fork, local build, custom Expo Module)
- [Source: docs/planning-artifacts/architecture.md#Architectural Boundaries] — `shared/services/ffmpeg.ts` as sole FFmpeg access point
- [Source: docs/planning-artifacts/architecture.md#Project Directory Structure] — `plugins/with-ffmpeg.js` location
- [Source: docs/planning-artifacts/architecture.md#Cross-Platform Strategy] — fork must support iOS for Phase 2
- [Source: docs/planning-artifacts/prd.md#Processing Pipeline] — `-skip_frame nokey` low-res keyframe pattern
- [Source: docs/planning-artifacts/prd.md#Contraintes Techniques] — RAM ≤ 2 GB on Poco X5 reference device
- [Source: docs/planning-artifacts/epics.md#Story 2.2] — original BDD acceptance criteria
- [Source: src/shared/services/ffmpeg.ts] — current implementation (works once dep is resolved)
- [Source: src/features/video-processing/processingPipeline.ts] — downstream consumer; contract must remain stable
- [Source: src/features/video-processing/types.ts] — `KeyframeInfo` shape lock
- [Commit `0c75f5d`] — explains why `with-ffmpeg` was removed from `app.json`
- [Commit `7eef587`] — original Sprint 2 scaffolding context

## Dev Agent Record

### Agent Model Used

Amelia (Developer Agent) — Claude Opus 4.7 (`claude-opus-4-7`)

### Debug Log References

- `npm view ffmpeg-kit-react-native` → confirmed deprecated (arthenica/tanersener, Jan 2025), still on npm at 6.0.2
- `npm view @jdarshan5/ffmpeg-kit-react-native` → 404 (not on npm — was JitPack-only and the JitPack 6.0-2 artifact does not resolve, exactly as commit `0c75f5d` documented)
- `npm search ffmpeg-kit` → surfaced active forks: `@wokcito/ffmpeg-kit-react-native@6.1.2` (3 months old), `@spreen/ffmpeg-kit-react-native@6.0.3` (a year old), `@apescoding/ffmpeg-kit-react-native@1.0.8` (5 months old)
- `npm pack @wokcito/ffmpeg-kit-react-native` + tarball inspection → its `android/build.gradle` declares `implementation 'io.github.jamaismagic.ffmpeg:ffmpeg-kit-main-16kb:6.1.4'` (Maven Central, 16-kb page-aligned for Android 15+ compatibility) and exports `FFmpegKit`/`FFprobeKit` as named exports — drop-in compatible with the existing service wrapper
- `npm install` → 1135 packages added cleanly
- `npm run typecheck` → 3 pre-existing errors in `firebaseConfig.ts`/`storage.ts` (verified on baseline `git stash`); zero new errors from this story
- `npm test` → 13/13 pass

### Completion Notes List

- **FFmpeg library chosen + Maven coordinate that actually resolved:** `@wokcito/ffmpeg-kit-react-native@6.1.2` → resolves to Maven Central artifact `io.github.jamaismagic.ffmpeg:ffmpeg-kit-main-16kb:6.1.4` (the package's own `android/build.gradle` declares the AAR; React Native autolinking pulls it in). 16-kb page-aligned, required for Android 15+ on the Poco X5 reference device class.
- **Whether `with-ffmpeg.js` config plugin was needed or simplified to no-op / deleted:** **Deleted entirely.** Per Dev Notes "Second preference", a no-op plugin is worse than no plugin. The fork auto-links via React Native autolinking, so no Gradle injection is required. `app.json` `plugins` array remains `[]`.
- **Result of `expo prebuild --clean` + `expo run:android`:** **Not run in agent sandbox** — Android SDK + device required. Handoff item for the developer to run locally.
- **Smoke test: keyframe count + first/last `timestampMs` for the test video used:** **Not run in agent sandbox** — physical device required. Handoff item.
- **Peak RAM during extraction (device, video duration, peak MB):** **Not measured** — Android Studio Profiler / `adb shell dumpsys meminfo` required. Handoff item (target < 2 GB per NFR5).
- **Anything that needed escalation (Plan B/C, alternative library, etc.):** No escalation needed. Plan A succeeded — the wokcito fork resolves the JitPack deadlock by routing through Maven Central instead. No vendored AAR (Plan B) or custom Expo Module (Plan C) required.
- **Why story status is NOT moved to `review`:** ACs 3, 4, and 6 are device-bound smoke tests that the agent cannot execute. The story's own Tasks/Subtasks block (Task 6 final bullet) explicitly requires "Update Story 2.2 status to `done` only after all ACs verified on a real device". Status remains `in-progress` pending hands-on verification.

### Code Review fixes applied (AI, 2026-05-04)

Adversarial code review on this branch surfaced 14 findings (3 HIGH, 6 MEDIUM, 4 LOW). Fixed in code:

- **H1 (shell-injection / path-quoting):** `extractKeyframes`, `extractFrameAt`, `getVideoDuration` all switched from `execute(string)` (shell-tokenized) to `executeWithArguments(string[])` so file paths cannot inject ffmpeg flags.
- **H2 (AC1 not actually verified):** Task 1 demoted from `[x]` → `[~]` (partial); the AC is met only after Task 3 runs Gradle on a real device.
- **H3 (AC2 stale wording):** AC2 reworded to admit the autolinking-only path now in use; Change Log records the rewording.
- **M1 / M2 (timestamp↔file desync, fabricated zeros):** Pairing now intersects `pts_time` log entries with the actual on-disk `frame_NNNN.jpg` file list and throws with a clear message when either set is empty. No more silent `timestampMs:0` fallback that would corrupt downstream Story 2.3 / 2.5 detection.
- **M3 (`cleanupProcessingFiles` path traversal):** `assertSafeSessionId(/^[a-zA-Z0-9_-]+$/)` enforced at both `getProcessingDir` and `cleanupProcessingFiles` entry points.
- **M4 (interop typed `any`):** Replaced with `FFmpegKitApi` / `FFprobeKitApi` / `FFmpegSession` interfaces covering only the surface the wrapper actually uses; `getFFmpeg` now also throws a distinct error if the module loads but exports are missing (catches export-shape regressions in the wokcito fork).
- **M5 (zero unit tests):** Added `src/__tests__/ffmpeg.test.ts` covering missing-module, misshapen-export, sessionId guard, and the executeWithArguments-not-shell-string contract.
- **MED-A (sprint-status omitted from File List):** Added.
- **L1 (header comment version mismatch):** Now references FFmpeg-kit 6.1.4.
- **L2 (`-y` flag missing):** Added to `extractFrameAt` and `extractKeyframes`.
- **L3 (`ensureDir` TOCTOU):** Now wraps `create()` in a benign try/catch.
- **L4 (`%04d` collision on re-run):** `extractKeyframes` now `clearDir`s the prior `keyframes/` output before re-extracting.

Deferred:

- **M6 (sprint-status convention drift):** Mixed `completed` / `not_started` / `in-progress` across sibling stories. Out of scope for a single-story fix; tracked as a sprint-level normalization task. Story 2.2 keeps `in-progress` (matches BMAD code-review workflow's status-sync output).

### Device handoff — what the developer must run next

```bash
# 1. Confirm the install is reproducible from a clean clone
rm -rf node_modules android && npm install

# 2. Generate the native Android project
npx expo prebuild --clean

# 3. Build and boot the dev client on a connected device/emulator
npm run android
# → Expect Gradle to download `io.github.jamaismagic.ffmpeg:ffmpeg-kit-main-16kb:6.1.4`
#   from Maven Central. Expect app to boot without the TypeError from commit 0c75f5d.

# 4. Smoke-test keyframe extraction (Task 4)
#   - Drop a ≥30 min H.264/AAC mp4 in device Download/
#   - Wire a temporary HomeScreen debug button to call:
#       extractKeyframes(uri, 'smoke-test-session') → console.log count + first/last
#   - Confirm count ≈ duration_sec / GOP and timestamps monotonic
#   - Confirm .jpg files exist under cacheDir/processing/smoke-test-session/keyframes/
#   - Capture peak RAM via Android Studio Profiler → Memory (target < 2 GB)
#   - DELETE the debug button before committing
#   - Record peak RAM in Completion Notes above

# 5. Once all of the above pass, mark Tasks 2 (final bullet), 3, 4, 6 [x] and
#    update Status to `review`.
```

### File List

- `package.json` — added `@wokcito/ffmpeg-kit-react-native: ^6.1.2` dependency
- `package-lock.json` — regenerated by `npm install` to lock the new dep tree
- `plugins/with-ffmpeg.js` — **deleted** (no longer needed; autolinking handles native AAR)
- `src/shared/services/ffmpeg.ts` — switched `require()` target to `@wokcito/ffmpeg-kit-react-native`; replaced shell-tokenized `execute(string)` with `executeWithArguments([...])` to remove path-quoting/injection risk; replaced `any`-typed module ref with a minimal `FFmpegKitApi` / `FFprobeKitApi` interface; added export-shape sanity check + sessionId path-traversal guard; replaced log-fail fallback that fabricated `timestampMs:0` with explicit throw; added `clearDir` of prior `keyframes/` output before re-extraction; added `-y` overwrite flag to single-frame extraction; updated header comment to record the FFmpeg-kit 6.1.4 AAR. Public surface (functions, types, return shapes) unchanged.
- `src/__tests__/ffmpeg.test.ts` — **new** Jest tests covering: missing-module error path, misshapen-export error path, `executeWithArguments` (not shell-string) is used so paths with `"` cannot inject flags, `getProcessingDir` cache-path composition, `assertSafeSessionId` accepts UUIDs, `cleanupProcessingFiles` refuses path-traversal sessionId.
- `docs/stories/2.2.md` — Task 1 marked `[~]` (partial pending Task 3 device verification); AC2 reworded to admit autolinking strategy; File List + Change Log updated; review-fix log added to Dev Agent Record.
- `docs/sprint-status.yaml` — Sprint 2 + Story 2.1 + Story 2.2 status fields updated by the implementing commit (now documented here for completeness; the story body had previously omitted this file).

## Change Log

| Date       | Change                                                                                                | Author |
|------------|-------------------------------------------------------------------------------------------------------|--------|
| 2026-05-04 | Switched FFmpeg native dep to `@wokcito/ffmpeg-kit-react-native@6.1.2` (Maven Central, 16-kb aligned) | Amelia |
| 2026-05-04 | Deleted `plugins/with-ffmpeg.js` — autolinking obviates manual Gradle injection                       | Amelia |
| 2026-05-04 | Service-boundary audit + typecheck + jest pass; device-side ACs (3, 4, 6) deferred to dev handoff    | Amelia |
| 2026-05-04 | AC2 reworded to admit the autolinking-only path used by the chosen library                            | Amelia |
| 2026-05-04 | Code-review fixes applied to `src/shared/services/ffmpeg.ts`: `executeWithArguments` (no shell injection), typed FFmpeg interop, sessionId path-traversal guard, fail-loudly on log/file desync, `-y` overwrite, prior-output cleanup. Added `src/__tests__/ffmpeg.test.ts`. | Amelia (CR) |
