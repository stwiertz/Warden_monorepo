/**
 * MMKV storage service.
 * Provides the MMKV instance and typed helpers for key-value storage.
 * Used for auth cache, session state auto-save, and user preferences.
 *
 * Key conventions: dot.notation (e.g., auth.token, session.current.position, prefs.exportQuality)
 */

// TODO: Implement MMKV instance and typed helpers
export {};
