/**
 * FFmpeg service wrapper.
 * Single access point for all FFmpeg operations (keyframe extraction, demux/mux, export).
 * Uses jdarshan5/ffmpeg-kit-react-native fork.
 */

// TODO: Implement FFmpeg wrapper once ffmpeg-kit-react-native fork is installed
export {};
