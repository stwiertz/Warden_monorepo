/**
 * Minimap ROI crop view (FR14-15).
 * Displays the cropped region of interest showing the minimap.
 */

// TODO: Implement minimap ROI crop view
import { View } from 'react-native';

export function MinimapView() {
  return <View />;
}
