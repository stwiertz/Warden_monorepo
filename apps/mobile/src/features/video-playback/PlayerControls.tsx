/**
 * Player controls component (FR11-15).
 * Play/pause, seek, timeline scrubber.
 */

// TODO: Implement player controls
import { View } from 'react-native';

export function PlayerControls() {
  return <View />;
}
