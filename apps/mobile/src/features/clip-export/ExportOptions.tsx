/**
 * Export options component (FR21-23).
 * Quality selection (Mobile 720p / HD source), clip range.
 */

// TODO: Implement export options UI
import { View } from 'react-native';

export function ExportOptions() {
  return <View />;
}
